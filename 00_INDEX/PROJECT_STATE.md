# PROJECT_STATE — Howner / ARKO

> **Source de vérité canonique** de l'état projet (ADR-019). Lu à la reprise (`/resume`, `/memory-sync`).
> Mettre à jour la section « Dernier point » en fin de session. Ne pas dupliquer cet état ailleurs.

## Résumé exécutif
Site **bi-produit** de réservation — **Arko One** (20 m²) + **Arko Max** (40 m²), ADR-022, sur un **pool commun de 6 exemplaires** numérotés 1→6 (`SERIE_TOTAL`, One et Max confondus — ce n'est pas 6 + 6 ; **ramené de 12 à 6 le 2026-08-04**, arbitrage de Richard). **Front complet et validé** (Lighthouse 100/100/100/100, LCP 0.8s, CLS 0). **Portail admin livré et en production ; portail mandataire ⏸ suspendu** (ADR-028). **Configurateur v2 livré sur `/configurer/v2`** (`noindex`), l'entonnoir public y mène entièrement — le CTA final **n'a pas de handler** (ADR-031). **Lancement commercial bloqué par le légal.**

> ✅ **Réaligné le 2026-08-19** — PR #75 (`dev` → `main`, 33 commits) mergée, `main` = `29a96b4a`, production déployée depuis `main` et vérifiée en ligne. L'anomalie du 2026-08-18 (production promue depuis `feat/adr-035-crm-leads`, `main` figé au 20/07) est **résorbée**. **Leçon conservée : l'état déployé se lit sur les alias Vercel, pas seulement sur les branches git** — un `promote` court-circuite `main` sans laisser de trace côté git.
>
> 🟠 **L'entonnoir mène au v2 et son CTA final reste sans handler — assumé par Richard le 2026-08-19.** Ce n'est plus un blocage : c'est une décision. La réservation se fait par la ligne téléphonique en attendant ADR-031. ⚠ **Le guardrail d'ADR-030 (« cet état ne doit pas atteindre `main` ») est de ce fait caduc et doit être amendé.** ⚠️ **Voir « Dernier point » 2026-07-13 — CGV non confirmées avocat déjà live en prod, liées au tunnel de réservation.** Sources de vérité : `src/lib/site.ts` (marque/pricing), `src/lib/configurateur/config.ts` (grilles v2), `DESIGN.md` (charte), `03_DECISIONS/` (ADR).

## État actuel
- Phase 1 (front) livrée et validée. **Refonte multi-pages bi-produit** (Arko One + Arko Max) livrée le 2026-06-16 (ADR-020/021/022). Phase 1.5 (SEO) métadonnées par page posées, reste sitemap/robots/JSON-LD. Phase 4 (backend) non démarrée. Légal bloqué.
- Charte **Affinity** appliquée le 2026-06-16 (ADR-002) — **à valider par Albert** (contredit le verrou « Argile & Encre » du PASSATION).
- MCP configurés : `github` (remote officiel), `supabase` (`ahfhownerdb`, read-only), `vercel` (OAuth). CLI Higgsfield + skills installés.

## Stack décidée

| Couche | Technologie | ADR |
|---|---|---|
| Framework | Next.js 16.2.9 App Router (Turbopack), React 19, TS | 001 |
| UI | Tailwind v4 (`@theme`), gsap, lenis, framer-motion | 001 |
| 3D | three / r3f / drei — **`/viewer` only** | 006 |
| Charte | Affinity (remap `@theme`) — `DESIGN.md` | 002 |
| Data (Phase 4) | Supabase `ahfhownerdb` (ref `msrjocrcewvqkcehruny`) | 007 |
| Paiement (Phase 4) | **Hors-ligne** (Stripe retiré MVP — ADR-008 amendé 2026-06-27) | 008 |
| Hébergement | Vercel | 023 |

## Produit
**Bi-produit** (ADR-022) — registre `PRODUCTS` (`src/lib/site.ts`) :
- **Arko One** — 20 m², emprise 6,65 × 3,60 m, **77 900 € TTC** (grille §5 de la spec, ADR-029).
- **Arko Max** — 40 m² (= ARKO historique), emprise 4,00 × 11,00 m, **99 900 € TTC** (grille §5 de la spec, ADR-029).

> Prix de base corrigés le 2026-07-31 (ADR-029). Le **reste de la grille** — paliers de terrasse, 6 options dont 3 structurelles, transport par zone — relève d'ADR-030 : le modèle de calcul actuel (`perM2`, `terrassePerM2`, livraison au km) est remplacé, pas ajusté. Le nombre d'exemplaires est à **Série 01 = 6 unités** depuis le 2026-08-04 (arbitrage de Richard, ADR-029 § Amendement) — `SERIE_TOTAL` et `serie.unites` alignés à 6. Les **créneaux en base** restent à ADR-031.

Devis 3 couches (maison + livraison + frais terrain), **logique verrouillée** (ADR-005/020), montants par produit. Configurateur avec sélecteur One/Max (`/configurer?produit=`). Réservation d'un numéro avec acompte 1 500 €. Pas de multi-segment, pas de catalogue. Wordmark ARKO retiré de l'accueil ; noms produits conservés.

## Phases & backlog

| Phase | Périmètre | État | ADR |
|---|---|---|---|
| Phase 1 — Front | design, conversion, média, perf | ✅ Livré | 001,005,006 |
| Phase 1.5 — SEO | sitemap/robots/OG/JSON-LD | 🟢 P0+P1 livrés (2026-06-17) ; **fix HTML front indexable en prod (PR #54 → `dev`, PR #55 → `main`, 2026-07-20)** ; P2 polish restant | 018 |
| Phase 4 — Backend | Stripe, Supabase, terrain, leads | ⛔ Non démarré — volet terrain/mandataire ⏸ **suspendu** (ADR-028) | 007→013, 028 |
| Pré-lancement — Légal | acompte/arrhes, CGV | ⛔ Bloqué (avocat) | 015 |

## Carte feature → fichier → état → cible

| Feature | Fichier | État | Cible | ADR |
|---|---|---|---|---|
| Réservation + acompte | `Reservation.tsx` `submit()` | `setSent(true)` — paiement hors-ligne (Stripe retiré MVP) | Supabase lead + acompte offline | 008 |
| Slots / jauge | `Reservation.tsx`, `Gauge.tsx` | statique 4/12 | Supabase Realtime | 007,009 |
| Liste d'attente | `Reservation.tsx` `<Waitlist/>` | `setSent(true)` | insert Supabase | 010 |
| Analyse terrain (zonage) | `LandTool.tsx` | BAN réel + zonage heuristique | GPU/IGN réel | 011 |
| Analyse via lien d'annonce | `LandTool.tsx` mode `annonce` | dégradé | Apify + BAN | 012 |
| Contact terrain (annonce) | `LandTool.tsx` (~L392) | `setSent(true)` | lead Supabase | 013 |
| Pack Terrain submit (configurateur) | `Configurator.tsx` `PackTerrainContactForm`, `Reservation.tsx` `submit()` | ⏸ **suspendu** — mode « pack » retiré, `/api/recherche-terrain` en 404 | réactivation par flag | 025, 028 |
| Portail mandataire (landing, auth, dossiers, contrat, terrains) | `src/app/(mandataire)/**` | ⏸ **suspendu** — 404 via layout de groupe | réactivation par flag | 028 |
| Admin — affectation lead ↔ mandataire + GED mandataire | `leads/[id]/page.tsx`, `AssignMandataire.tsx`, `LeadDocuments.tsx` | ⏸ **suspendu** — sections masquées, routes en 404 | réactivation par flag | 027, 028 |
| Admin — écrans Mandataires / Affectations / GED / Terrains | `(admin)/admin/(protected)/{mandataires,affectations,ged,terrains}`, **`src/proxy.ts`** | ⏸ **suspendu** — 404 au **proxy** (layout parent client : `notFound()` seul ne coupait pas) + gardes en page | réactivation par flag | 028 |
| Public — `/terrains`, `/rechercheterrain`, `/terrain`, `/cgu-mandataire` | pages `(public)/**` | ⏸ **suspendu** — 404, hors sitemap, en `disallow` | réactivation par flag | 018, 028 |
| Devis 3 couches multi-produit (v1) | `Configurator.tsx`, `config-store.tsx` | ✅ sert toujours `/configurer` — remplacé à terme par le v2 | retrait à la bascule | 005,020 |
| **Configurateur v2** | `src/app/(configurateur)/**`, `src/components/configurateur/*`, `src/lib/configurateur/*` | ✅ livré sur **`/configurer/v2`** (`noindex`) — colonne de sections, grilles en données, ambiances visuelles, coque sans nav | bascule sur `/configurer` + levée du `noindex` | 030, 031 |
| **CRM — dashboard de suivi** | `(admin)/admin/(protected)/page.tsx`, `shared/components/admin/{StatutsCommerciauxDonut,ConseillersBar}.tsx` | ✅ livré (branche) — indicateurs de retard, charge par conseiller, table de priorité | — | 035 |
| **CRM — liste + Kanban** | `(admin)/admin/(protected)/leads/page.tsx`, `components/admin/LeadsVue.tsx` | ✅ livré (branche) — colonne « Affectation » retirée, `?vue=kanban` | — | 035, 028 |
| **CRM — journal d'appels** | `components/admin/LeadAppels.tsx`, `api/admin/leads/[id]/appels` | ✅ livré (branche) — table `lead_appels`, trigger `dernier_appel_at` | migration à appliquer | 035 |
| **CRM — configuration v2 sur le lead** | `components/admin/LeadConfiguration.tsx`, `src/lib/crm.ts` | ✅ livré (branche) — `config_v2` + `cfg_*`, libellés résolus par `loadConfig()` | alimenté par ADR-031 | 035, 030 |
| **CRM — GED Client double origine** | `components/admin/LeadClientDocuments.tsx` | ✅ livré (branche) — `origine` ahf/client, `categorie`, pièces manquantes | dépôt client = ADR-034 | 035, 027 |
| Email confirmations contact | `src/app/api/contact/route.ts`, `src/lib/email.ts` | ✅ Brevo template `10` — câblé | — | 026 |
| Email recap configurateur/terrain | `src/app/api/recherche-terrain/route.ts` | ✅ Brevo template `9` — câblé | — | 026 |
| Analyse terrain (adresse + PLU) | `src/components/site/ParcelleAnalyse.tsx`, `src/app/api/parcelle/route.ts` | BAN → apicarto geom → GPU coords | — | 011,025 |
| Stockage leads + consentement PLU | `src/app/api/reservation/route.ts`, `src/lib/supabase.ts` | Non-bloquant, lazy client | Migration preprod/prod | 007 |
| Calcul livraison GPS | `src/components/site/Reservation.tsx`, `src/lib/site.ts` (TRANSPORT) | ✅ Haversine × road_factor × poids × tarif — réactif via `plu_result_updated` | Coordonnées atelier à affiner | — |
| SEO | `sitemap.ts`, `robots.ts`, `opengraph-image.tsx`, `viewer/layout.tsx`, `lib/jsonld.ts`, `seo/JsonLd.tsx`, `llms.txt/route.ts` | ✅ P0+P1 (sitemap/robots/OG/twitter/canonical/noindex + JSON-LD Org/Product/FAQ + llms.txt) | P2 polish | 018 |
| **Page « À propos »** | `(public)/a-propos/page.tsx`, `ABOUT` (`site.ts`), `aboutPageSchema()` | ✅ livré (branche) — composant serveur, contenu en données, JSON-LD `AboutPage`, entrée `NAV` + sitemap + `llms.txt` | — | 037, 029 |

## Risques principaux

| Risque | Impact | Gravité | ADR |
|---|---|---|---|
| ~~Prix publics faux (59 900 / 89 900 €)~~ ✅ corrigés le 2026-07-31. ~~Réservation affichée 5 000 € vs 2 000 € au §7~~ ✅ **alignée à 2 000 € le 2026-08-02** (`DEPOSIT_EUR`, FAQ et réassurance interpolées ; variable Vercel absente des 3 scopes, le fallback fait foi) | Commercial | ⚪ Levé | 029 |
| **Entonnoir de réservation sans handler final** (CTA v2) — **assumé par Richard le 2026-08-19**, réservation par téléphone jusqu'à ADR-031. Guardrail ADR-030 à amender | Conversion | 🟠 Assumé | 030, 031 |
| **⚠️ CGV non confirmées avocat (`f3de62fe`) déjà LIVE en prod** (`/cgv`, liée au checkbox d'acceptation de `Reservation.tsx`) — engagement contractuel possible sur texte non validé | Risque juridique direct | 🔴 Critique | 015 |
| Légal acompte/arrhes + CGV non validés | Pas de vente | 🔴 Critique | 015 |
| RLS Supabase mal configurée | Fuite leads/réservations | 🔴 Critique | 007 |
| Charte Affinity non validée Albert | Dérive identité vs PASSATION | 🟠 Moyenne | 002 |
| API terrain externes (GPU/IGN/Apify) | Feature dégradée | 🟠 Moyenne | 011,012 |

## Variables d'environnement — état par scope Vercel

```
# Supabase — 3 scopes Vercel (Production / Preview / Development)
# ✅ Production : configuré (2026-06-27) → ahfhownerdb (msrjocrcewvqkcehruny)
# ⏳ Preview    : à configurer → ahfhownerdb-preprod (à créer sur dashboard Supabase)
# ⏳ Development: à configurer → ahfhownerdb-dev ou Supabase local CLI
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=         # serveur, jamais commité

# Stripe — RETIRÉ DU MVP (ADR-008 amendé 2026-06-27 — paiement hors-ligne)
# STRIPE_SECRET_KEY=              ← ne pas configurer
# STRIPE_WEBHOOK_SECRET=          ← ne pas configurer
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY= ← ne pas configurer

# Brevo — ✅ Production + Preview configurés (ADR-026)
BREVO_API_KEY=                     # serveur, jamais commité
BREVO_SENDER_EMAIL=contact@affinityhousefactory.com
BREVO_SENDER_NAME=Howner - By Affinity House Factory
BREVO_TO_AHF=contact@affinityhousefactory.com
BREVO_TEMPLATE_CONTACT=10
BREVO_TEMPLATE_RECAP=9

# Transport livraison (valeurs par défaut dans site.ts)
NEXT_PUBLIC_DELIVERY_GRUTAGE_EUR=1440

# Feature flags (ADR-028)
# Domaine « Mandataire & Terrain » SUSPENDU : ne PAS définir cette variable.
# L'absence vaut suspension (défaut sûr). Seule la chaîne exacte "true" active.
# Mode d'emploi (activer / re-suspendre / vérifier) : docs/feature-flags.md
# NEXT_PUBLIC_FEATURE_MANDATAIRE=true   ← ne pas configurer

# CRM interne (ADR-035) — facultatives : absentes = repli du code
# NEXT_PUBLIC_CRM_CONSEILLERS=Richard,Albert,Accueil   ← liste des conseillers AHF
# NEXT_PUBLIC_CRM_SLA_JOURS=7                          ← seuil d'alerte « sans nouvelle »

# Divers (Phase 4+)
APIFY_TOKEN=
ANTHROPIC_API_KEY=                 # optionnel (ADR-017)
```
Montants déjà en env (`NEXT_PUBLIC_RESERVATION_DEPOSIT_EUR`, `NEXT_PUBLIC_ARKO_BASE_EUR`) — ADR-003.

## Index ADR (`03_DECISIONS/`)

| ADR | Titre | Statut | Faisabilité |
|---|---|---|---|
| 001 | Stack Next.js 16 App Router | Accepté (livré) | ✅ |
| 002 | Charte Affinity (`@theme`) | **Accepté — valider Albert** | ⚠️ |
| 003 | Secrets & montants via env | Accepté (partiel) | ✅ |
| 004 | Règles de marque absolues | **Remplacé → ADR-029** | ✅ |
| 005 | Configurator/pricing verrouillé | **Remplacé → ADR-030** | ✅ |
| 006 | Guardrails perf & média | Accepté (guardrail) | ✅ |
| 007 | Supabase schémas + RLS | Proposé | ✅ |
| 008 | Réservation paiement (hors-ligne — Stripe retiré MVP) | **Différé** | ⚪ |
| 009 | Jauge/slots Realtime | Proposé | 🟠 |
| 010 | Waitlist insert | Proposé | ✅ |
| 011 | LandTool zonage GPU/IGN | Proposé | 🟠 |
| 012 | LandTool annonce Apify | Proposé | 🟠 |
| 013 | Contact terrain → leads | Proposé | ✅ |
| 014 | Service email transactionnel | **Remplacé → ADR-026** | ✅ |
| 015 | Légal acompte/arrhes/CGV | **Bloqué (avocat) — ⚠️ nouvelle CGV (`f3de62fe`) déployée en prod le 2026-07-13 SANS confirmation avocat obtenue** | 🔴 |
| 016 | Échéancier 10/30/40/20 % | Différé | 🟠 |
| 017 | Enrichissement terrain Anthropic | Différé (option) | ⚪ |
| 018 | Socle SEO | **Accepté — P0+P1 livrés** | ✅ |
| 019 | Gouvernance cognitive INDEX/HUB/_RUNTIME | Accepté | ✅ |
| 020 | Configurateur multi-produit (amende 005) | **Remplacé → ADR-030** | ✅ |
| 021 | Architecture multi-pages + nav Tesla | Accepté | ✅ |
| 022 | Split produit One/Max + repositionnement | **Accepté — valider Albert** | 🟠 |
| 023 | Déploiement production Vercel | Proposé | ✅ |
| 024 | Bandeau consentement cookies + Cloudflare Turnstile | Accepté | ✅ |
| 025 | Page `/rechercheterrain` — recherche personnalisée de parcelles | Accepté — **⏸ suspendue (028)** | ✅ |
| 026 | Emails Brevo templates dashboard + Supabase contacts | **Accepté — livré** | ✅ |
| 027 | Refonte fiche Lead admin — recherche terrain, affectation géo, GED double | **Accepté — livré ; affectation + GED mandataire ⏸ suspendues (028)** | ✅ |
| 028 | **Suspension réversible du domaine « Mandataire & Terrain »** | **Accepté — livré** | ✅ |
| 029 | **Repositionnement produit & marque** — cadre de vente, vocabulaire, prix (remplace 004) | **Accepté — lot 1 livré** | ✅ |
| 030 | **Configurateur v2** — grilles pilotées par données (remplace 005 et 020) | **Accepté — parcours livré sur `/configurer/v2` (`dev`, 2026-08-02) ; amendé (colonne de sections, coque dédiée) ; bascule sur `/configurer` conditionnée à 031** | 🟠 |
| 031 | Soumission de la demande de numéro (configurateur v2) | **Réservé — à écrire** ; hérite du contrat de données d'ADR-035 | ❓ |
| 032 | Dossier terrain (qualification, uploads, rendez-vous) | Réservé — à écrire | ❓ |
| 033 | Back-office des grilles tarifaires (`loadConfig()` → base) | Réservé — à écrire | ❓ |
| 034 | Espace client (dépôt de pièces) | Réservé — à écrire ; GED prête côté CRM (`origine = 'client'`) | ❓ |
| 035 | **Refonte du CRM interne** — suivi commercial, journal d'appels, capture configurateur v2, Kanban, GED double origine | **Accepté — livré sur `feat/adr-035-crm-leads` (2026-08-04)** ; **amendé le 2026-08-04** : « Lead chaud » → « Paiement réservé », règle de blocage du numéro de série, connecteur Pennylane à écrire | ✅ |
| 036 | **Synchronisation Pennylane** — le statut « Paiement réservé » posé depuis l'encaissement réel (MCP/API) | **Réservé — à écrire** ; dépendance externe critique, **alerte Albert** | ❓ |
| 037 | **Page « À propos »** (`/a-propos`) — ADN de marque, sans partenaire nommé | **Accepté — livré (`dev`, 2026-08-17)** ; **alerte Albert** : nom du bureau d'études retiré (ADR-029 §67) + slug à confirmer | ✅ |
| 038 | **19 pages éditoriales SEO** (usages, guides, local) — registre de routes, arbitrage des URL en collision, régime vocabulaire | **Accepté — lots 0, 1 et 2 livrés : 5 pages d'usage publiées (13 URLs au sitemap)** ; restent le hub + 9 guides (lot 3) et les 4 pages locales (lot 4, **en attente de matière locale**) ; **alertes Albert** : contenus réglementaires à joindre au dossier avocat, **CGV toujours rédigées en « maison » / CCMI** (ADR-015) | 🟠 |

## Prochaines priorités (actionnable sans blocage externe)
1. ~~**Merger `feat/terrain-address-lookup`**~~ ✅ mergé 2026-06-27.
2. ~~**`PackTerrainContactForm` submit câblé**~~ ✅ câblé 2026-06-27.
3. ~~**Brevo contacts opt-in (DOI)**~~ ✅ livré 2026-06-29 — PRs #12+#13 mergées sur main.
4. ~~**`feat/admin-portal` Étapes 1→6**~~ ✅ livré 2026-06-30 — PR #14 mergée sur dev.
5. ~~**Portail mandataire redesign + auth**~~ ✅ livré 2026-06-30 — PR #16 mergée sur dev.
6. ~~**PR #17 `dev` → `main`**~~ ✅ mergée 2026-06-30. Migrations prod appliquées : `20260629_admin_tables` + `20260630_mandataires_invitation`.
7. ~~**ADR-026 SPF/DKIM prod**~~ ✅ corrigé manuellement (2026-07-10) — n'est plus un bloqueur externe.
8. ~~**`fix/scrape-annonce-error-logging` → `dev`**~~ ✅ mergée (PR #51) — inclut ADR-027 (fiche lead), refonte CGV (en attente confirmation avocat), révision blocklist marque ADR-004, refonte FAQ/hero/promesse/réassurance, import photos terrain, extraction IA Anthropic enrichie, contact Brevo direct.
9. ~~**Migration `20260710_lead_client_documents.sql`** (GED Client, ADR-027)~~ — ✅ appliquée Preview (`ixozlavseaykxmjtkkrk`) le 2026-07-10 **et Prod (`msrjocrcewvqkcehruny`) le 2026-07-13** (PR #24 `dev`→`main`). Preview et Prod alignés.
10. **CGV nouvelle version** (`f3de62fe`) — en attente confirmation avocat avant passage en production (ADR-015 reste bloqué sur ce seul point).
11. ~~**Fix HTML front indexable**~~ ✅ livré en prod 2026-07-20 (PR #54 → `dev`, PR #55 → `main`). **ADR-018 SEO reste P2** (polish non bloquant).
    - **Placeholders Brevo `LEAD_DESCRIPTION` / `LEAD_PRODUIT`** à ajouter au template `BREVO_TEMPLATE_AFFECTATION` (15) — reliquat ADR-027.
12. ~~**Configurateur v2 — parcours et écrans**~~ ✅ livré sur `dev` le 2026-08-02 (`/configurer/v2`, `noindex`). **Suite actionnable** :
    - **ADR-031** — soumission de la demande de numéro : le bouton « Réserver ce numéro » n'a pas encore de handler (`onAction` vide). Sans lui, la bascule sur `/configurer` casserait l'entonnoir.
    - **Bascule `/configurer` + levée du `noindex`** — à faire *après* ADR-031, avec retrait de `Configurator.tsx` / `config-store.tsx` (v1).
    - ~~Volume de série~~ ✅ retranché le 2026-08-04 : **6 unités** (annule le « reste à 12 » du 02/08), ADR-029 et ADR-030 amendées.
    - ~~Alerte Albert~~ ✅ traitée verbalement le 2026-08-02 — écarts maintenus et assumés.
13. **ADR-035 — CRM interne** ✅ livré sur `feat/adr-035-crm-leads` (2026-08-04). **Suite actionnable** :
    - ~~Pousser la branche et ouvrir la PR vers `dev`~~ ✅ **PR #73 ouverte le 2026-08-04**, en attente de revue.
    - ~~Appliquer `20260804_crm_leads.sql` sur Preview~~ ✅ **appliquée le 2026-08-04** sur `ahfhownerdb-preprod` (`ixozlavseaykxmjtkkrk`), structure et **trigger vérifiés fonctionnellement**, données de test retirées. **⚠ Un défaut de sécurité a été relevé par l'audit Supabase et corrigé dans la foulée** (fonctions `SECURITY DEFINER` exposées en RPC à `anon`) — audit revenu propre.
    - **Vérifier le Kanban et le journal d'appels sur la Preview Vercel** (le reste n'est pas testable en local).
    - **Appliquer `20260804_crm_leads.sql` sur Prod** à la validation de la PR `dev` → `main` — **inclure le correctif de sécurité** (`security invoker` + `revoke`), il est dans le même fichier.
    - **Appliquer `20260804_statut_paiement_reserve.sql` sur Prod** dans la même passe, **après** la précédente (renommage `chaud` → `paiement_reserve`). ✅ appliquée et **vérifiée par requête** sur Preview le 2026-08-04 (contrainte relue, 1 ligne migrée). Prod n'a **aucun lead** : l'`update` y sera sans effet, seule la contrainte change.
    - ⚠ **Deux objets de base contredisent la règle du numéro de série** — `leads_slot_unique` (bloque le numéro dès le premier lead, quel que soit son statut) et `leads_slot_check` (encore `BETWEEN 1 AND 12`). **Non corrigés**, à trancher avec ADR-031 : ADR-035 § Amendement, point 3.
    - Renseigner au besoin `NEXT_PUBLIC_CRM_CONSEILLERS` et `NEXT_PUBLIC_CRM_SLA_JOURS` sur Vercel (absentes = repli du code : « Richard, Albert, Accueil » et 7 jours).
14. **Arko Max pricing grid** (perM2/options/terrasse/footprint/`reserved`) — toujours en attente données métier + grille tarifaire. ⚠ Ne concerne plus que le configurateur **v1** : le v2 n'utilise ni `perM2` ni `terrassePerM2`.
15. **Coordonnées atelier** : placeholder Bayonne (43.4933 / −1.4748) — à affiner. Sert désormais aussi le calcul de transport du v2.

## Tokens / MCP
Rotation tokens GitHub + Supabase **différée** → `memory/token-rotation-pending.md`.

## Dernier point

**2026-08-20 (ADR-038 lot 2 — les cinq pages d'usage, PR #79)** — 107 Ko de copy d'Albert deviennent cinq pages : `/studio-jardin-haut-de-gamme`, `/bureau-de-jardin`, `/dependance-habitable`, `/bureau-pour-teletravail`, `/studio-jardin-tiny-house`. **Vérifiées sur le HTML servi par la Preview avant d'être déclarées au sitemap**, jamais l'inverse.

**Ce que la vérification a montré** : un seul `<h1>` par page et conforme au registre, 9 à 13 `<h2>` sans saut, **zéro `opacity:0` sérialisé** — les cinq pages sont lisibles sans JS, donc indexables. `BreadcrumbList` partout ; `FAQPage` sur les **trois** pages qui portent une FAQ réellement visible, sur aucune autre. Vocabulaire propre sur le texte rendu, « tiny house » cantonné à la page qui compare (24 occurrences, zéro ailleurs). **Le sitemap de cette Preview annonçait encore 8 URLs** : les pages étaient servies sans être déclarées — exactement ce que le champ `statut` doit produire. Elles sont passées à `"publiee"` ensuite : **13 URLs**.

**Les blocs éditoriaux sont nés de la première page, pas devinés avant elle** (ADR-038 §8). Neuf formes couvrent les cinq specs. Tous serveur, `Reveal` seul îlot client, et **aucun `<h1>` sous un bloc animé en JS** — la règle actée le 19/08 tient. La matrice comparative a été **généralisée à N colonnes après la troisième page** : le comparatif One/Max en veut deux, la comparaison « pièce intérieure / extension / studio » en veut trois ; deux composants presque identiques auraient divergé au premier ajustement.

**Un défaut que seule la vérification a rendu visible : les pages auraient été orphelines.** Entrer au sitemap sans aucun lien entrant, c'est demander l'indexation de pages que rien ne référence. Le pied de page reçoit donc une colonne « Usages » **dérivée du registre** — servie sur toutes les pages publiques, elle s'étoffera seule aux lots suivants et ne rend rien tant que la famille est vide. La **navigation principale n'est pas touchée** : 19 entrées ne tiennent pas dans une barre, et le placement dans le méga-menu est un arbitrage de présentation qui revient à Richard.

**Le copy a été réécrit, jamais exempté.** Les 92 termes proscrits des specs sont reformulés : « maison » désignant l'habitation du visiteur devient « votre habitation », le « container habillé » devient « abri de chantier repeint », la « construction modulaire » devient « fabrication hors-site ». **Le garde-fou m'a repris trois fois pendant l'écriture** — deux fois sur mes propres commentaires (je citais les termes que je remplaçais) et une fois sur un « catalogue de surfaces » que j'avais écrit moi-même dans une phrase neuve. *Quatrième illustration de la même leçon : un contrôle n'a de valeur que s'il s'applique aussi à celui qui l'a écrit.*

**Une exception a dû être étendue à un endroit qu'elle ignorait.** « tiny house » était levé sur la page et le registre ; le copy vit dans `src/lib/pages/contenu/`, hors périmètre de l'exception. Chemin ajouté, **ADR-029 amendée** en conséquence : une exception doit suivre le texte là où il est réellement écrit. Sur ces trois fichiers, tout le reste de la blocklist continue de s'appliquer.

**Prudence tenue sur trois fronts, tous exigés par les specs** : aucune performance chiffrée (isolation, acoustique, thermique — rien n'est validé au dépôt) ; **aucun argument « sans permis »**, les pages disent l'inverse ; aucune promesse de valorisation immobilière. Le cadre de vente d'ADR-029 est respecté — la dépendance habitable est une **annexe sur parcelle bâtie**, jamais un logement sur terrain nu.

⚠ **Deux pages traitent un sujet voisin** — `/bureau-de-jardin` (l'objet : ossature, modèles, journée type) et `/bureau-pour-teletravail` (la situation : profils, alternatives, organisation). Le partage est documenté en tête du fichier de contenu et la section constructive n'est pas dupliquée mais liée. **À rejuger sur le rendu** : si elles se recouvrent trop, la fusion reste ouverte — même réserve que pour les guides 01/04/07.

**Aucun média ajouté** : les deux visuels demandés par le classeur étaient déjà au dépôt depuis le 19/08. Budget d'ADR-006 inchangé. **Les visuels Arko One propres à ces pages restent à traiter** dans un lot média séparé, sur demande de Richard.

**Gate** : `tsc` propre · `eslint` propre sur les fichiers créés, **2 erreurs avant / 2 après** sur `Footer.tsx` (dette préexistante) · `check:vocabulaire` conforme.

**⚠ Un défaut d'accessibilité introduit par ces pages, signalé par Richard et corrigé** — la barre de menu restait **transparente** tant que la page n'avait pas défilé de 24 px. Le parti tenait tant que toutes les pages ouvraient sur un fond clair (accueil et pages produit posent leur hero sur `canvas`) ; **les pages éditoriales ouvrent sur un hero `ink`**, où les libellés en `text-ink/70` devenaient du texte sombre sur fond sombre. Nom de la marque, « Nos Studios », « À propos » et « Contact » étaient **invisibles au chargement** et ne réapparaissaient qu'au défilement. **Corrigé à la source** : la barre porte son fond clair en permanence et ne dépend plus de ce qu'il y a derrière elle — l'écouteur de défilement et son état disparaissent avec. C'est ce qui évite que la prochaine page à hero sombre reproduise le défaut. **⚠ Portée : tout le site** — sur l'accueil et les pages produit, la barre gagne un filet et un fond translucide dès le haut de page, là où elle était nue. Changement visuel léger mais réel, assumé par la demande. Vérifié sur le HTML servi : `bg-canvas/80 backdrop-blur-md` inconditionnel sur les deux types de page.

*Leçon : un composant global dont l'apparence dépend du fond de la page qu'il surplombe est un piège qui ne se déclenche qu'à la première page dérogeant à l'habitude. Ici, elle est arrivée cinq fois d'un coup.*

**2026-08-20 (ADR-038 lot 1 — audit de conformité des pages produit au `contexte-refonte.md`)** — Périmètre **réduit sur demande de Richard** : vérifier la conformité, **ne pas toucher aux visuels**. Vérifié **sur le HTML servi en production**, pas sur le code — la seule lecture qui ait valeur de preuve depuis le 2026-08-02.

**Conforme sur six points sur sept.** Les **trois `<h1>` dictés** sont servis au mot près, **un seul par page**, sur l'accueil comme sur les deux pages produit. Les **routes renommées** répondent en **308** depuis `/arko-one` et `/arko-max`. Le menu porte « **Nos Studios** ». Le **texte rendu** des pages commerciales (accueil, deux produits, `/a-propos`, `/contact`, `/configurer`, `/configurer/v2`, `/confidentialite`, `/viewer`) ne contient **aucun terme proscrit**. Les **surfaces invisibles mais servies** — métadonnées, Open Graph, JSON-LD, `llms.txt` — sont propres elles aussi : c'est là que le repositionnement du 19/08 aurait pu laisser des restes, et il n'en a pas laissé.

**Un écart mineur corrigé** : les **mentions légales** présentaient encore « les maisons compactes d'architecte Arko One et Arko Max ». Phrase descriptive, pas une définition contractuelle — corrigée en « studios de jardin d'architecte ». La corriger **réduit** l'exposition CCMI, elle ne l'augmente pas.

**⚠ Un écart majeur relevé et volontairement NON corrigé — les CGV.** Elles emploient **39 fois « maison »**, dont **« Maison ARKO » en terme contractuel défini** (section Définitions, repris dans tout le document), « maisons légères ARKO » au pied de page, et **deux renvois explicites au régime de la construction de maison individuelle**.

**Ce n'est pas un problème de vocabulaire, c'est une contradiction entre le discours et le contrat.** Le repositionnement du 19/08 avait pour effet favorable d'**éloigner le site du régime CCMI** (risque 🔴 ouvert depuis le 03/08). Or le contrat, lui, parle toujours de maisons légères et cite le CCMI. **En cas de litige, c'est le contrat qui est lu, pas la page d'accueil** : le bénéfice juridique du repositionnement est donc, en l'état, largement annulé par les CGV.

**Non corrigé à dessein**, pour trois raisons : c'est un **document contractuel**, « Maison ARKO » y est un **terme défini** dont dépendent 39 occurrences, et la version live **n'est toujours pas validée par l'avocat** (ADR-015). Réécrire un contrat non validé sans son auteur juridique aggraverait le risque au lieu de le réduire. **À porter au dossier avocat avec les CGV — c'est une alerte Albert.**

*Note de méthode : les pages légales sont exclues de `check:vocabulaire` (ADR-029, périmètre §17.10). L'exclusion est légitime, mais elle a une conséquence qu'il faut nommer — **aucun garde-fou automatique ne surveille le vocabulaire du contrat**. Cet écart ne pouvait être trouvé qu'en lisant les pages servies, ce que le lot 1 a fait.*

**2026-08-19 (mise en production — PR #76, `dev` → `main`, 3 commits, visuels Arko Max)** — `main` = **`4b2fb554`**, déploiement production `dpl_Hdsfcbg…` **READY en 54 s**, aliasé sur `howner.fr`, `www.howner.fr`, `affinityhome.fr` et `affinityhousefactory.com`. **Aucune migration Supabase**, aucun changement de logique métier — média et présentation seulement.

**Ce qui est passé** : vidéo produit propre à l'Arko Max (accueil + page dédiée) et **huit rendus AVIF** (1,8 Mo) fournis par Richard, qui remplacent les films Higgsfield génériques sur `/studio-jardin-arko-max`. La section « La Révélation » **ne scrube plus une vidéo mais une séquence d'images** pilotée par le scroll — l'extérieur descend et se pose, le zoom traverse la façade, on arrive dans la cuisine, un panoramique vers la gauche découvre le séjour. « Découvrir » passe de **sept à six vues** (« L'heure bleue » retirée : le film de crépuscule ne montre pas l'Arko Max). Le parcours sert la vue en clairière.

**Le point d'architecture qui compte** : `RevealScrub` et `Discover` sont **partagés avec l'Arko One**. Ils prennent désormais leurs médias en **props** (`frames`, `panels`), les données vivant dans `src/lib/media/arko-max.ts` ; **sans prop, le comportement est inchangé**. C'est ce qui permet de refondre un produit sans toucher l'autre — et ce qui devra être répété quand l'Arko One recevra enfin ses propres rendus (il sert encore la vidéo 40 m² avec sa mention « visuel provisoire »).

**Vérifié en ligne sur le corps servi, pas sur les codes HTTP** : `/studio-jardin-arko-max` sert les **7 rendus Max** attendus (le 8ᵉ, le séjour, n'est monté que côté client par la séquence) et **plus aucun** des anciens médias — `aerien`, `crepuscule`, `bedroom`, `bathroom`, `arko-rear`, `arko-forest` ont tous disparu de la page ; compteur **« 01 / 06 » → « 06 / 06 »**, « 6 vues ». **Non-régression de l'Arko One vérifiée sur la même base** : sa page sert toujours ses six anciens médias et `film-scrub`, **aucun asset `arko/max` n'y apparaît**, compteur « 01 / 07 » → « 07 / 07 ».

**Deux réglages de rendu documentés dans le code**, parce qu'ils ne se devinent pas : (1) la vue extérieure est jouée **deux fois** dans la séquence, une fois pour la pose au sol, une fois pour le zoom d'entrée — le `to` du premier plan égale le `from` du second, donc le fondu entre deux images identiques est invisible et le mouvement paraît continu ; déplacer un `at` sans reprendre le chevauchement voisin casse le fondu. (2) `sizes` suit l'**échelle maximale de chaque plan** — un plan qui zoome à 1,85 doit recevoir une image 1,85 fois plus large que la fenêtre, sinon Next en sert une au format écran et le zoom la rend molle ; d'où l'unique source encodée en 3840 px.

**Perf (ADR-006) tenue** : les transforms de la séquence s'écrivent **en DOM direct**, zéro rendu React par frame, et l'état de progression est quantisé — au passage la boucle existante rend **moins** qu'avant. Mobile et `prefers-reduced-motion` : image fixe plein cadre, aucun scrub.

**Gate** : `tsc` propre · `check:vocabulaire` conforme · eslint **6 erreurs avant / 6 après** sur le périmètre touché (préexistantes) · **Preview `0a86007d` READY avant merge** — le build Vercel reste le seul vrai typecheck, aucun build local (contrainte de la machine).

⚠ **Inchangé par cette mise en production, toujours vrai** : l'entonnoir mène au configurateur v2 dont le CTA final n'a pas de handler (assumé le 19/08, réservation par téléphone jusqu'à ADR-031), et les **CGV restent live sans validation avocat** (ADR-015). **Le guardrail caduc d'ADR-030 n'est toujours pas amendé.**

**2026-08-19 (mise en production — PR #75, `dev` → `main`, 33 commits)** — **`main` était figé au 20/07 depuis 30 jours.** La PR aligne enfin git sur la réalité et **redevient la source du déploiement de production** : l'anomalie du 18/08 — production promue depuis `feat/adr-035-crm-leads`, `main` court-circuité — est résorbée. `main` = `29a96b4a`. Bascule constatée **30 s** après le merge.

**Deux décisions de Richard prises à cette occasion, toutes deux assumant un risque connu.** (1) **L'entonnoir continue de mener au v2**, dont le bouton final n'a pas de handler : le repli d'une ligne vers le v1 fonctionnel a été **explicitement écarté**. Ce n'est plus un défaut subi mais un choix — la réservation passe par la ligne téléphonique jusqu'à ADR-031. ⚠ **Conséquence de gouvernance : le guardrail d'ADR-030, « cet état ne doit pas atteindre `main` », est caduc et doit être amendé** (non fait — un ADR requiert son accord). (2) **Les CGV partent en production sans validation avocat**, décision maintenue depuis le 13/07 (ADR-015).

**Ce qui est passé en production** : repositionnement « studio de jardin » (ADR-029 § Amendement du 19/08), routes produit renommées avec redirections 308, trois nouveaux `<h1>`, page `/a-propos` (ADR-037) et sa photo d'ossature LSF, bloc d'identification NAP au pied de page, CRM interne (ADR-035, déjà en ligne via le promote mais désormais aligné sur git), et les correctifs de silence du 18/08. **Aucune migration à appliquer** : les deux fichiers apparaissent au diff mais étaient déjà passés sur Prod le 18/08.

**Vérifié en ligne sur le corps servi, pas sur les codes HTTP** : `/arko-one` et `/arko-max` répondent **308** vers les nouvelles URL ; les quatre pages rendent 200 avec les bons titres ; **un seul `<h1>` par page, aucun sous un parent opaque** ; vocabulaire conforme sur les quatre textes rendus ; « Nos Studios » présent, aucun « Maisons » résiduel ; bloc NAP servi ; sitemap à 8 URLs ; image LSF en `image/webp`. Les 12 CTA pointent bien sur `/configurer/v2`, conformément à la décision (1).

**⚠ Une régression introduite puis corrigée avant la mise en ligne, trouvée par la Preview et non en local** : en déplaçant le `<h1>` des pages produit vers le bandeau bas, il était entré dans un `motion.div` porteur d'`opacity:0` — framer-motion sérialise cet état, le titre partait donc **invisible sans JS et pour un crawler**. C'est le défaut du 2026-07-20, dont le correctif d'alors n'avait pas atteint `ProductHero` (son `<h1>` n'était pas encore dans ce bloc). Remplacé par `Reveal`, dont l'état masqué vit en CSS sous `.js-motion` : **9 `opacity:0` en production avant → 8 après**, une de moins qu'auparavant. **Règle actée en commentaire : aucun `<h1>` ne doit naître sous un bloc animé par framer-motion.**

**Le garde-fou vocabulaire a par ailleurs rattrapé ma propre prose** — « conteneur » écrit au sens HTML dans un commentaire, alors que le mot est proscrit au sens maritime. Le contrôle n'arbitre pas le sens, et c'est précisément ce qui le rend fiable. *Incident de méthode à retenir : un commit a été poussé alors que ce contrôle échouait, les deux commandes n'étant pas chaînées — le gate doit conditionner le commit, pas le précéder.*

**2026-08-19 (⚠ repositionnement — « studio de jardin » remplace « maison », routes produit renommées)** — **Consigne permanente de Richard**, pas un lot ponctuel : le site ne vend plus une maison, il vend un **studio de jardin premium / d'exception**. Plus de « construction », de « maison à bâtir » ni de « construction de maison individuelle ». **C'est l'inverse exact de l'amendement du 2026-08-03**, qui avait fait de « maison » le terme imposé — les deux mouvements sont datés dans ADR-029 plutôt qu'effacés, l'historique explique le code qu'on lit. **73 occurrences** réécrites hors périmètres exclus, avec **retour du féminin au masculin** partout : `BRAND.madeIn` redevient « Fabriqué au Pays-Basque », le pied de page « Conçu », les promesses « livré prêt à vivre ». L'étape 01 du parcours passe de « **Construite** entière » à « **Fabriqué** entier » — le mot évoquait précisément le régime dont la marque se démarque. **Garde-fou réarmé** : `maisons?` retourne dans `PROSCRITS`, et la ligne CCMI y est **volontairement redondante** pour qu'un futur relâchement de « maison » n'emporte pas le garde-fou juridique en silence. **✅ Effet juridique favorable, à porter au dossier avocat** : ce repositionnement **éloigne** le site du régime **CCMI** (loi du 19 déc. 1990), risque 🔴 ouvert depuis le 03/08 — un studio de jardin n'est pas une maison individuelle. **Premier mouvement de ce projet qui réduit une exposition juridique au lieu de l'augmenter.** **Trois `<h1>` dictés par Richard** : accueil, Arko One, Arko Max. **Le `<h1>` des pages produit n'est plus le nom du produit** — il portait « Arko One » en très grand, il porte désormais la catégorie, que le nom ne dit pas (personne ne cherche « arko one »). Le nom reste dominant à l'œil mais passe en `<p>`, motif déjà employé pour la baseline du pied de page et les libellés du méga-menu ; un seul `<h1>` par page. **`BRAND.h1` est distinct de `BRAND.baseline`** : le titre nomme catégorie et marque pour le référencement, la baseline reste la signature éditoriale — la ponctuation même diffère, ils ne se déduisent pas l'un de l'autre. **⚠ Faute d'accord dans un `<h1>` dicté, signalée et non corrigée** : « Studio de jardin de 40 m² d'architecte, **livrés prêts** à vivre. » accorde au pluriel un sujet singulier (repli de « Deux maisons … livrées prêtes »). Repris tel quel — c'est de la copie de marque. Une ligne à changer si validé. **Routes renommées** `/arko-one` → `/studio-jardin-arko-one` et `/arko-max` → `/studio-jardin-arko-max`, avec **redirections permanentes** (`permanent: true` = **308**, traité comme un 301) dans `next.config.ts` : ces URL sont au sitemap depuis juin, les renommer à sec aurait jeté le référencement acquis et servi un 404 à tout lien entrant. **Ces redirections ne s'enlèvent pas.** Slug centralisé dans `PRODUCTS[].slug`, donc Nav, Footer, showcase et JSON-LD ont suivi sans retouche ; seuls sitemap et canonical le codaient en dur. **Menu « Maisons » → « Nos Studios ».** Titres SEO des pages produit repris (« Studio de jardin 20 m² — Arko One »). **Un piège évité** : `${SERIE_TOTAL}` inséré dans une chaîne simple n'aurait pas été interpolé — passé en gabarit. **Périmètre inchangé** : pages légales, domaine mandataire suspendu et back-office restent hors contrôle, leur vocabulaire est encore l'ancien. **Gate** : `tsc` propre (après purge de `.next`, dont les types générés pointaient encore les anciennes routes), `check:vocabulaire` conforme, eslint **318 erreurs avant / 318 après**. **Reste** : vérification Preview, et le reste de la refonte demandée par Richard (nouvelles pages à générer).

**2026-08-18 (⚠ la production ne servait pas `main`, base Prod en pause, migrations CRM appliquées)** — Parti chercher si le CRM tournait en prod sans ses migrations. Trois défauts trouvés, emboîtés.

**1) La production ne sert pas `main`, et personne ne le savait.** Le déploiement Vercel `target: production`, aliasé sur `howner.fr`, `www.howner.fr`, `affinityhome.fr` et `affinityhousefactory.com`, porte `githubCommitRef: feat/adr-035-crm-leads`, SHA **`c06fc1c3`**, `action: "promote"` — promu le **2026-08-07**. `main` est bien resté à `4d34ed26` : le garde-fou « cet état ne doit pas atteindre `main` » a été respecté **sur git** et contourné **par le côté**. Vérifié sur le HTML servi, pas déduit : `howner.fr` rend **12 CTA vers `/configurer/v2`** et affiche « 6 exemplaires » — or `SERIE_TOTAL = 6` n'existait **que** sur la branche CRM (`dev` portait 12). **L'entonnoir de réservation est donc mort en production depuis le 7 août** : le bouton final est un `onAction` à corps vide. Pendant ce temps le sitemap déclare `/configurer` (le v1), qu'aucun CTA ne dessert. **Règle actée : l'état déployé se lit sur les alias Vercel, pas sur les branches git.** `PROJECT_STATE` décrivait la production comme `main` au 20/07 — corrigé au résumé exécutif.

**2) La base de production était en pause.** `ahfhownerdb` (`msrjocrcewvqkcehruny`) en statut **`INACTIVE`** — ce que trois « timeouts » successifs avaient masqué. Richard l'a réactivée. **Et l'échec était silencieux des deux côtés** : `/api/reservation` enferme l'insert dans un `try`, le `catch` ne fait qu'un `console.error` (« Non bloquant — l'email est envoyé même si le stockage échoue ») puis la route renvoie `{ ok: true }` **inconditionnellement** ; `/api/contact` fait de même. Un visiteur voyait donc une **confirmation de succès** pendant que rien ne s'enregistrait, AHF recevant seulement l'email Brevo. Les erreurs runtime Vercel sur 7 jours : **aucune** — cohérent, un `console.error` dans un `catch` ne remonte pas. **Ampleur** : `leads` = 0 ligne ; `contacts` = 6 lignes, toutes écrites dans une même fenêtre d'une heure le **7 juillet**, plus rien depuis. Impossible de distinguer « écritures perdues » de « pas de trafic » ; le profil de ces 6 lignes ressemble à des tests. Perte probablement nulle à faible — ce qui était cassé, c'est la **capacité** à enregistrer.

**3) Le CRM tournait contre un schéma vide de ses objets.** Avant correction : **0 / 17** colonnes CRM sur `leads`, `lead_appels` **absente**, `origine`/`categorie` **absentes**. La requête exacte de `/admin/leads` échouait en `42703: column "responsable" does not exist` — **exécutée sur la prod pour le prouver**, pas supposée. Et le code fait `const { data } = await …` **sans lire `error`** : `data` valait `null`, `(data ?? [])` rendait une **liste vide**. L'écran affichait « aucun lead » au lieu de signaler la panne. **Troisième forme silencieuse de la même journée**, après les deux routes API — et cinquième occurrence de la leçon du projet : un garde-fou qui n'observe pas la sortie réelle ne contrôle rien. Ici c'est la **réponse rendue à l'utilisateur** qui ment.

**Migrations appliquées sur Prod le 2026-08-18**, dans l'ordre imposé, sur accord de Richard : **`20260804_crm_leads.sql`** puis **`20260804_statut_paiement_reserve.sql`**. **Vérifié par requête et non sur `success: true`** : 17/17 colonnes, `lead_appels` créée, 2/2 colonnes GED, **6/6 index**, trigger `trg_lead_appels_sync` présent, RLS active + 1 politique ; contrainte relue — `chaud` remplacé par `paiement_reserve` (0 lead, l'`update` fut sans effet, seule la contrainte a changé) ; **la requête de l'écran admin passe désormais** et renvoie `[]`. **Correctif de sécurité contrôlé explicitement** : `leads_recalc_dernier_appel` en `INVOKER`, `leads_sync_dernier_appel` en `DEFINER` (normal, fonction de trigger), et **`anon` comme `authenticated` ne peuvent exécuter ni l'une ni l'autre**. `get_advisors` security ne remonte qu'un WARN préexistant et sans rapport (protection mots de passe compromis désactivée, réglage Auth). **Preview et Prod sont désormais alignées.** ⚠ **Le trigger n'a PAS été testé fonctionnellement sur Prod** — il l'avait été sur Preview le 04/08, et le faire ici aurait exigé d'écrire des lignes de test en production. Structure vérifiée, comportement non rejoué : c'est une réserve, pas une preuve.

**`leads_slot_check` laissé en `BETWEEN 1 AND 12`** sur Prod, sciemment : le durcir à 6 relève d'ADR-031, qui doit d'abord trancher où vit l'unicité du numéro (le numéro confirmé, pas le souhait du lead). `leads_slot_unique` contredit toujours `etatNumeroPourStatut()`.

**Correctif du silence appliqué le même jour** (décision de Richard : « traiter le 2, admettre le reste comme bon »). Traité comme **un** défaut de conception, pas comme trois bugs : la règle vit dans **`src/shared/lib/panne.ts`**, qui la formule une fois — *une opération qui échoue sans bloquer doit laisser trois traces : un journal repérable, une réponse qui ne prétend pas au succès de ce qui a échoué, un écran qui distingue « rien à afficher » de « je n'ai pas pu lire »*. Quatre surfaces alignées dessus. **Les routes restent non bloquantes par choix** — si le stockage tombe, l'email part quand même et AHF reçoit la demande ; lever ferait perdre le lead. Ce qui est corrigé, c'est le silence, pas le repli. `/api/reservation` renvoie désormais `{ ok, persisted }`, `/api/contact` renvoie `{ success, persisted, notified }` — trois faits distincts, trois champs, là où un booléen unique confondait « demande reçue » et « demande enregistrée ». **L'envoi Brevo de `/api/contact` était lui aussi avalé** : les deux canaux pouvant tomber ensemble, le visiteur voyait un succès pour une demande qui n'existait nulle part — corrigé au passage, c'est le même défaut dans le même fichier. Côté écrans, **`ErreurRequete`** remplace la liste vide et le tableau de bord à zéro : le message brut de la base y est affiché tel quel (écran interne — c'est `column "responsable" does not exist` qui fait gagner le diagnostic). **Préfixe `[PANNE]`** sur tous les journaux : `console.error` n'ouvre pas de grappe d'erreurs Vercel, un préfixe constant les rend au moins filtrables. ⚠ **Limite assumée et documentée** : l'alerte ne peut pas être portée dans l'email AHF, seul canal qu'un humain lise vraiment — `sendBrevoTemplate` envoie **un seul** message avec les **mêmes paramètres** au client *et* à AHF, un avertissement s'afficherait donc au client. Le porter proprement demande un paramètre `ALERTE_STOCKAGE` ajouté au template dans le tableau de bord Brevo, puis une ligne de code. **Non fait plutôt que fait à moitié.** Les 5 autres `const { data } = await` du dépôt sont dans le domaine mandataire suspendu (ADR-028) : non touchées. **Gate** : `tsc` propre, `check:vocabulaire` conforme, eslint **20 erreurs avant / 20 après** sur le périmètre admin.

**Reste ouvert** : (a) l'entonnoir mort en production — le repli `RESERVER_PATH → /configurer` tient en une ligne, mais demande un redéploiement ; (b) récupérer d'éventuelles demandes dans la boîte Brevo pour la période concernée ; (c) le paramètre `ALERTE_STOCKAGE` côté template Brevo.

**2026-08-17 (ADR-029 § Amendement — identification & contact au pied de page, pour le SEO local)** — **Décision de Richard** : le pied de page porte « **Howner by Affinity House Factory** · 28 Chemin de Sabalce OEV, 64100 Bayonne · Tél. : +33 (0)5 64 37 37 14 ». **⚠ Le bloc nomme une raison sociale autre que Howner**, ce que le §2 d'ADR-029 interdit et que son §4 (« Une seule identité ») exclut côté client — **exception nommée, datée et bornée à ce bloc**, ADR-029 amendée et son §4 marqué. L'exception tient parce que c'est la limite que l'ADR **se connaissait déjà** : son point ouvert §3 constatait que « une seule identité » se heurte à l'obligation légale de nommer l'entité réelle et « ne peut pas être appliqué à la lettre sans validation juridique ». Deux motifs s'ajoutent : (1) **un NAP n'a de valeur que cohérent** — le JSON-LD `Organization` émettait déjà `legalName: "Affinity House Factory"` et l'adresse de Bayonne sur **toutes** les pages, et `llms.txt` annonçait déjà l'éditeur ; afficher Howner seule aurait mis le texte visible en contradiction avec les données structurées, ce que les moteurs sanctionnent ; (2) « Howner **by** AHF » garde la marque en premier, la hiérarchie du §4 est préservée en substance. **L'interdiction de nommer un fournisseur, sous-traitant ou partenaire reste entière** — c'est elle qui a fait retirer le nom du bureau d'études de la page « À propos » le même jour (ADR-037). **Mise en œuvre** : constante **`COMPANY`** dans `site.ts`, **source unique** — le pied de page l'affiche, `jsonld.ts` en **dérive** `Organization.address` et `legalName` au lieu de les écrire en dur ; rendre l'adresse visible sans la centraliser aurait créé deux vérités et c'est exactement le défaut qu'un NAP ne pardonne pas. Rendu en **`<address>`** (l'élément désigne précisément les coordonnées du propriétaire du document, lu des moteurs comme des lecteurs d'écran), numéro **cliquable** en `tel:` depuis `CONTACT` — toujours surchargeable par `NEXT_PUBLIC_CONTACT_PHONE`, absente de Vercel, c'est le repli qui sert. **`PhoneLink` non réutilisé** : stylé pour fond clair (`text-ink/70`, infobulle sombre), il serait illisible sur le pied de page sombre. **Portée réelle : toutes les pages publiques**, pas seulement l'accueil — le pied de page est porté par le layout `(public)`, et c'est aussi la bonne pratique NAP, une adresse limitée à l'accueil affaiblissant le signal. Casse en Titre et non en CAPITALES (`DESIGN.md` impose la sentence case ; les pages légales portent déjà cette graphie). **Non gardé par un script** : `check:vocabulaire` ne teste que des termes produit, pas les raisons sociales — l'exception ne tient que par l'amendement. **À remonter à Albert** : le §4 est de sa main, l'exception doit être confirmée ou le bloc retiré (une constante à changer). **Gate** : `tsc` propre, `check:vocabulaire` conforme, eslint **2 erreurs avant / 2 après** sur `Footer.tsx` (`d'architecte`, `d'intention` — dette pré-existante, aucune régression).

**2026-08-17 (ADR-037 — page « À propos » sur `/a-propos`)** — Branche `feat/page-a-propos`, partie de `dev` (`96f084f0`), **PR dédiée**. Le site n'avait aucune page de marque : l'accueil et les pages produit vendent un objet, rien n'exposait la démarche, la technologie structurelle ni l'argument éco — un manque de conversion autant que de SEO, « qui est Howner » n'ayant pas de page de destination. Texte source d'Albert (`page_about_howner.md`, Drive `Siteaffinity/About`, déposé le jour même), **versé au dépôt** en `docs/specs/page-about-howner.md` pour être cité par un chemin stable, comme la spec configurateur. **⚠ Un écart assumé à ce texte** : il nommait le bureau d'études structure et posait un lien sortant vers son site, ce que le **§67 d'ADR-029 interdit sans réserve** (« Howner est la seule entité citée côté client. Aucun nom de fournisseur, de sous-traitant ou de partenaire n'apparaît, y compris dans les descriptifs techniques »). Le nom et le lien sont retirés, **l'argument est conservé** — « l'expertise d'un bureau d'études structure indépendant » : c'est la caution technique qui porte l'argument commercial, pas l'identité du prestataire. Formulation choisie pour rester **exacte** : « notre bureau d'études » aurait laissé croire à une équipe interne. **Décision d'Albert requise** — s'il veut le nom, ADR-029 §67 doit être amendée, ce n'est pas un arbitrage de mise en œuvre ; la remettre coûte une ligne de `site.ts`. **Second écart** : « nos architectes » ramené au singulier imposé (« notre architecte intégrée »), et « habitat » substitué là où la tournure laissait entendre un logement autonome — le cadre de vente du §1 ne bouge pas. **LSF, acier et Hors-Site sont employés sciemment** : sortis de la blocklist le 2026-07-09 (ADR-004 § révision), ne pas les relire comme des infractions. **Slug français `/a-propos` et non `/about`** : toutes les routes de contenu le sont et le public est français ; le dossier Drive s'appelle « About », ce qui décrit le sujet, pas l'URL — **à confirmer avant production**, une route indexée ne se renomme qu'au prix d'une redirection permanente. **Mise en œuvre** : contenu dans `ABOUT` (`site.ts`) et jamais dans le composant, comme `PROCESS` / `FAQ` / `LAND_PREP` ; **composant serveur**, `Reveal` en seul îlot client — l'état masqué vivant sous `.js-motion`, le HTML servi reste visible sans JS, donc indexable (leçon du 2026-07-20) ; **aucun `<main>` dans la page**, le layout `(public)` en fournit déjà un — `/contact` et `/arko-*` en ouvrent un second, imbriqué, défaut préexistant **non corrigé ici** et à joindre au polish P2 d'ADR-018. **Aucun média ajouté** : trois assets déjà au dépôt, le budget de page d'ADR-006 est inchangé. **Aucun prix ni palier** : la page ne touche pas `config.ts`, elle ne peut pas se périmer avec les grilles ; le bloc de sortie dérive de `SERIE_COUNT` / `SERIE_TOTAL`, aucun volume recopié. **SEO** : entrée `sitemap.ts` (0,7), « Pages clés » de `llms.txt`, JSON-LD `AboutPage` — pas d'`Organization` dupliquée, le layout l'émet déjà sitewide. **Navigation** : une seule entrée ajoutée à `NAV`, qui alimente d'un coup la barre desktop, le menu mobile et le pied de page. **Gate** : `tsc` propre, `eslint` propre sur les fichiers touchés, `check:vocabulaire` conforme. **Vérifié sur la Preview `dev` le 2026-08-17**, sur le HTML servi et non sur le code : plan des titres `h1` + 4 `h2` sans saut, **0 `opacity:0` sérialisé** (indexable sans JS), JSON-LD `AboutPage` présent, **0 occurrence** du nom du bureau d'études, `/a-propos` au sitemap et dans `llms.txt`, vocabulaire conforme sur le texte rendu de l'accueil comme de la page. Le bloc `<address>` est servi sur les deux. *Au passage, quatrième illustration de la même leçon : l'URL de Preview sans jeton renvoie **HTTP 200 en servant la page de login Vercel**.*

**2026-08-04 (« Pack prêt à louer » retiré de la grille)** — **Décision de Richard : l'offre n'est pas viable après étude.** L'option `pack_location` (1 990 € TTC, One et Max) disparaît de `config.ts`. Contenue à ce seul fichier — aucune surface publique ne la mentionnait. **La version de grille est incrémentée dans le même geste**, sans quoi une configuration antérieure serait relue avec la grille du jour et paraîtrait n'avoir jamais contenu l'option : c'est exactement le service que rend `cfg_version` (ADR-035 §4), et un garde-fou qu'on n'arme pas ne garde rien. Format **daté** plutôt que `v2` — la version des *grilles* n'est pas celle du *configurateur*, et « grille v2 » à côté du « configurateur v2 » aurait été illisible ; `"v1"` → `"2026-08-04"`, affiché tel quel dans la fiche. **Aucun lead ne portait l'option** — vérifié sur Preview par requête, pas supposé ; s'il y en avait eu, `resoudreConfigV2()` l'aurait restituée « hors grille » avec son identifiant brut plutôt que de la faire disparaître. **Règle actée dans ADR-030** : tout mouvement de prix, de palier ou d'option incrémente `version`. **Écart supplémentaire à la spec d'Albert** (§5 et JSON du §17 la listent encore) — la spec reste une source versionnée, non réécrite, comme pour les trois écarts précédents. **À porter à Albert avec les autres.** En sens inverse, l'écart « Série 01 à 12 unités » **n'existe plus** depuis le retour à 6. Gate : `tsc`, `eslint` et `check:vocabulaire` propres.

**2026-08-04 (ADR-035 § Amendement — « Paiement réservé », règle du numéro de série, Série 01 ramenée à 6)** — Trois arbitrages de Richard le jour même de la livraison du CRM. **1) « Lead chaud » → « Paiement réservé ».** Le statut ne décrivait rien de vérifiable : deux conseillers ne mettaient pas le curseur au même endroit, et une colonne de Kanban remplie d'appréciations ne pilote rien. Il constate désormais un **fait comptable** — l'encaissement de la réservation du numéro. **L'identifiant en base est renommé aussi** (`chaud` → `paiement_reserve`), pas seulement le libellé : le futur connecteur Pennylane écrira cette valeur, et garder `chaud` en base pendant que l'écran dit « Paiement réservé » aurait recréé la divergence que le §7 d'ADR-035 venait de supprimer. Migration **`20260804_statut_paiement_reserve.sql`** (drop CHECK → `update` → repose CHECK, l'ordre compte) **appliquée sur Preview et vérifiée par requête** : contrainte relue, 1 ligne migrée. Prod n'a **aucun lead** — l'`update` y sera sans effet. Teinte orange → teal : le Kanban se lit jaune (devis) → teal (payé) → vert (signé), au lieu d'un orange qui disait « urgent » là où il faut lire « acquis ». **2) Le numéro de série a deux niveaux de prise**, règle qui n'existait nulle part en code : rien avant « Devis envoyé » (plusieurs leads peuvent viser le même numéro) → **réservé** au devis (retiré des propositions, mais reprenable tant que rien n'est payé) → **bloqué** à l'encaissement, seul état qui décrémente le compteur public. C'est la formalisation du mode « demandé puis confirmé » d'ADR-030, qui ne disait pas **ce qui** déclenchait le passage. Codée une seule fois dans `etatNumeroPourStatut()` (`crm.ts`), jamais dupliquée. **Rendu** : badge `N° x` à côté du modèle dans la liste et sur les cartes du Kanban (`NumeroSerieBadge`), teinté selon le niveau — un conseiller qui arbitre entre deux leads sur le même numéro voit lequel a payé. ⚠ **Contrat, pas application** : `chargerNumeros()` reste statique, rien ne décrémente réellement tant qu'ADR-031 n'a pas posé la table. **⚠ Deux objets de base contredisent la règle, relevés et laissés en l'état** : `leads_slot_unique` bloque le numéro **dès le premier lead qui le choisit, quel que soit son statut** — l'inverse exact de la règle ; la contradiction est dormante tant que la soumission n'écrit pas, et deviendra bloquante au premier doublon (erreur d'insertion pour le second visiteur). Et `leads_slot_check` est encore `BETWEEN 1 AND 12`. À trancher avec ADR-031, qui portera le bon endroit pour l'unicité (le numéro confirmé, pas le souhait du lead). Durcir le CHECK à 6 **échouerait aujourd'hui sur Preview**, qui porte 1 ligne de test avec `slot > 6` — vérifié avant de renoncer, pas supposé. **3) Série 01 ramenée à 6 exemplaires**, ce qui **annule l'arbitrage du 2026-08-02** et remet le §5 de la spec en vigueur. Richard l'a demandé « dans le configurateur » ; **appliqué à tout le site après arbitrage explicite** — garder 12 en en-tête et 6 dans le sélecteur aurait produit l'incohérence contre laquelle le commentaire de `config.ts` mettait en garde. `SERIE_TOTAL = 6` et `serie.unites = 6` ; `BRAND.total` et `PRODUCTS.*.total` **dérivent** désormais de la constante au lieu de la recopier, et les trois derniers littéraux du site (fiche technique, `description` SEO d'`/arko-one`, bloc avant-première) ont été **interpolés** — le prochain changement de volume tient en une ligne. ADR-029 et ADR-030 amendées, les mentions du 02/08 barrées plutôt qu'effacées. **4) Pennylane : intention, pas décision.** `paiement_reserve` a vocation à être posé automatiquement depuis l'encaissement (MCP/API) ; d'ici là il reste saisissable à la main. **Le connecteur aura son propre ADR (036 réservé)** — authentification, appariement facture ↔ lead, fréquence, comportement en cas de remboursement : dépendance externe critique, donc **alerte Albert**. **Gate** : `tsc` propre, `eslint` propre sur les fichiers touchés, `check:vocabulaire` conforme.

**2026-08-04 (ADR-035 — refonte du CRM interne, chantier prioritaire)** — Branche **`feat/adr-035-crm-leads`** poussée, **PR #73 → `dev` ouverte** (23 fichiers, +3 181 / −421, **aucune migration appliquée**). Le portail `/admin` a été bâti pour le réseau de mandataires, suspendu depuis ADR-028 : dashboard amputé de ses widgets financiers, colonne « Affectation » sans titulaire, et **aucun outil de phoning** alors que c'est l'usage réel. En face, le configurateur v2 produit une configuration que le back-office ne sait pas lire. **Le CRM est refait d'abord, à dessein** : il définit le **contrat de données** que la soumission d'ADR-031 remplira — dans l'autre ordre, ADR-031 aurait improvisé un format et le CRM s'y serait adapté après coup. **Numérotation** : 031→034 sont réservés et cités dans ADR-030 comme dans le code ; les renuméroter aurait cassé une dizaine de renvois pour rien, d'où **035**. **Trois arbitrages de Richard** : (1) « affectation » redevient une notion interne — nouveau champ **`responsable`** (conseiller AHF), **sans rapport avec `mandataire_id`**, la colonne « Affectation » (champ `statut`) est retirée de la liste mais le champ reste en base et éditable ; (2) **deux définitions du retard, jamais confondues** — rappel daté dépassé (rouge) et silence > N jours sur un lead actif (orange, `NEXT_PUBLIC_CRM_SLA_JOURS`, défaut 7) ; **un lead jamais appelé compte son silence depuis sa création**, sans quoi les plus négligés seraient les seuls à n'alerter jamais ; (3) **journal d'appels manuel** — le clic « Appeler » ouvre le lien `tel:` *et* pré-ouvre la fiche d'appel, mais **rien n'est enregistré sans validation** : un journal qui se remplit tout seul se remplit d'appels qui n'ont pas eu lieu. **Capture du configurateur v2 en double écriture** : `config_v2` (jsonb, fidélité, **avec la version de grille** — un lead de mars ne doit pas se relire avec la grille de septembre) **et** colonnes plates `cfg_*` (ce sur quoi le CRM trie et agrège ; extraire un `jsonb` par ligne de liste serait payer cher une information lue à chaque écran). **Les libellés ne sont pas stockés** — résolus par `loadConfig()`, seule source des grilles (ADR-030) : stocker un libellé, c'est le figer. Le numéro reste **`slot`**, colonne existante à index unique ; l'état demandé/confirmé reste à ADR-031, et l'écran le dit explicitement. **GED Client** : `origine` ahf/client — l'espace client (ADR-034) écrira dans la même table, **le CRM est prêt avant lui** — plus `categorie` adossée à `PIECES_DOSSIER`, d'où l'avancement du dossier par différence (une pièce attendue sans document est une absence, pas une ligne en base). **Écrans** : dashboard reconstruit (5 indicateurs, table « à traiter en priorité » triée par urgence, répartition par statut commercial, **charge par conseiller** empilée) ; liste avec **Kanban** (`?vue=` lu **côté serveur** — leçon d'ADR-030 sur `useSearchParams`), glisser-déposer natif sans bibliothèque, **sélecteur de statut sur chaque carte comme chemin principal sur tactile**, HTML5 DnD n'y répondant pas ; fiche complétée de trois blocs (configuration, journal, GED enrichie) sans toucher à la colonne terrain/PLU d'ADR-027. **Les 8 statuts commerciaux passent dans `src/lib/crm.ts`** — ils étaient déclarés **trois fois**, avec des libellés et des couleurs **déjà divergents**. **Fuseau épinglé sur Europe/Paris** dans les formateurs : appelés depuis des composants client donc rendus d'abord sur serveur (Vercel en UTC), un appel de 00 h 30 s'affichait la veille. **Migration `20260804_crm_leads.sql` purement additive, NON appliquée** (Preview ni Prod). **Gate** : `tsc` propre, `check:vocabulaire` conforme, **eslint 21 → 20 erreurs** sur le périmètre admin (pas de régression, une dette de moins). **Migration appliquée sur Preview le jour même** : structure vérifiée (17 + 10 + 2 colonnes, 6 index, RLS + politique, trigger et 2 fonctions) et **trigger testé fonctionnellement** — la note ne compte pas comme contact, le max gagne, la suppression fait retomber sur le suivant, tout supprimer ramène à `NULL` ; données de test intégralement retirées. **⚠ L'audit Supabase a relevé un défaut que j'avais introduit** : les deux fonctions du trigger étaient `SECURITY DEFINER` dans le schéma `public`, donc **exposées en `/rest/v1/rpc/` au rôle `anon`** — une écriture sur `leads.dernier_appel_at` en contournement de RLS, pour tout lead dont l'UUID était deviné. Portée limitée (seule la valeur recalculée peut être écrite) mais primitive d'écriture inutile. **Corrigé** : `SECURITY INVOKER` sur la fonction interne (elle s'exécutait déjà avec les droits du propriétaire, appelée depuis un trigger `DEFINER`) + `revoke all … from public, anon, authenticated` sur les deux ; trigger re-testé après correctif, audit revenu propre. **Règle à retenir : dans Supabase, une fonction `SECURITY DEFINER` posée dans `public` est une route API publique tant qu'on ne lui retire pas l'exécution.** **Volet mandataire strictement inchangé**, toujours derrière `FEATURES.mandataire` ; le mode « Pack terrain » de la création de lead naît gardé. **Reste** : pousser la branche, ouvrir la PR, vérifier sur Preview (le trigger `dernier_appel_at` n'est pas testable en local), puis appliquer la migration à la validation `dev` → `main`.

**2026-08-03 (ligne d'appel, puis ⚠ bascule « module » → « maison »)** — **Mergé sur `dev` en fast-forward : `b7339d44..e284cac4`, 6 commits, 32 fichiers, aucune migration.** `main` reste à `4d34ed26`. **1) Ligne téléphonique** `+33 (0)5 64 37 37 14` en en-tête du site et du tunnel, plus un encadré « Contacter un conseiller » sur `/contact` (Lu–Ve 9 h–12 h / 14 h–18 h). Source unique `CONTACT` dans `site.ts` (surchargeable par `NEXT_PUBLIC_CONTACT_PHONE`, **absente des scopes Vercel — c'est le repli qui sert**), composant `PhoneLink` sans JS — infobulle en CSS pur. JSON-LD `Organization` enrichi de `telephone` + `contactPoint.hoursAvailable` en E.164. Le numéro a changé en cours de session : **une seule ligne modifiée**, les liens `tel:`, le JSON-LD et les trois surfaces d'affichage en ont dérivé — la source unique a tenu sa promesse. Tout vérifié sur le HTML servi. *Au passage : une sonde a renvoyé `200` en servant la **page de login Vercel** (jeton de partage périmé). Troisième illustration de la même leçon.* **2) ⚠ « module » → « maison » sur tout le site client** — **décision de Richard, alerte formulée et maintenue** : 70 occurrences réécrites avec passage au féminin, `maisons?` retiré de `PROSCRITS` et remplacé par `maisons? individuelles?`. L'accord était **déjà cassé par endroits** (« le module installée », « Construite entière ») : restes de l'époque « maison », désormais alignés. `BRAND.madeIn` passé à « Fabriquée au Pays-Basque » et bas de page à « Conçue » ; `/arko-one` garde le masculin, qui s'y accorde à *studio*. **ADR-029 amendée** (§2 barré + section datée portant l'alerte), `CLAUDE.md` et `AGENTS.md` réécrits. La **spec d'Albert n'est pas modifiée** — source versionnée ; son critère de recette §16 est signalé caduc sur ce point dans l'ADR. **Le cadre de vente du §1 ne change pas** — annexe ou hébergement professionnel, terrain nu toujours fermé. **Risque ouvert** : « maison » vendue à un particulier qui fournit son terrain touche au régime **CCMI** (loi du 19 déc. 1990) ; lecture de Claude non validée par un avocat — les CGV en production comportent déjà une **clause de renvoi conditionnelle** à ce régime. À confirmer avec les CGV (ADR-015). **À remonter à Albert.**

**2026-08-02 (suite — nettoyage des CTA et ⚠ un terme proscrit servi en production)** — Deux commits après la reprise de la FAQ (`d958ac0e`, `56012c04`, mergés sur `dev`). **CTA « Tester mon terrain » retiré** de l'accueil puis des deux pages produit : `/terrain` est suspendue (ADR-028) et son repli menait au configurateur **v1**, que plus aucun autre bouton ne dessert depuis que l'entonnoir est dirigé vers le v2. La vérification de parcelle vit dans le parcours lui-même, en section 05. Le `Hero` et le `ProductHero` ne portent plus qu'un CTA ; l'occurrence restante (`Process.tsx`, `/arko-max`) est une **ancre interne `#terrain`**, pas la route suspendue — laissée en place. **⚠ Découverte en ouvrant `ProductHero`** : « À partir de 99 900 € — **clé en main**, prête à vivre. » était servi sur `/arko-one` et `/arko-max`. Le terme est proscrit par ADR-029 — et `npm run check:vocabulaire` annonçait « conforme ». **Cause** : le contrôle lisait le source **ligne à ligne**, et le terme était coupé par un retour à la ligne JSX (`… € — clé\nen main`) ; aucune ligne ne le contenait, le rendu si. **Correctif** : le script analyse désormais le texte **tel que le visiteur le lit** (blancs aplatis) tout en rapportant la ligne d'origine — vérifié sur fichier témoin, aucun faux positif. Passage de toute la blocklist au crible avec cette lecture : **c'était la seule occurrence du dépôt**. Texte corrigé en « livré prêt à vivre » (l'accord féminin de « prête » était un reste de « maison »). **Règle actée dans ADR-029** : le vocabulaire s'apprécie sur le texte rendu, jamais sur le code. **Deuxième garde-fou de ce projet à rapporter un succès sur une surface non conforme**, après « un 404 ne prouve pas un masquage » (ADR-028) — même leçon : un contrôle qui n'observe pas la sortie réelle ne contrôle rien.


**2026-08-02 (ADR-030 — configurateur v2 : parcours livré sur `/configurer/v2`, mergé sur `dev`)** — La branche `feat/adr-030-configurateur-ecrans` est mergée sur `dev` en fast-forward (`0300237b..1bc955c4`, 6 commits, historique linéaire, **aucune migration**). Le parcours est en ligne sur **`/configurer/v2`** en `noindex` : `/configurer` continue de servir le tunnel v1 tant qu'ADR-031 n'a pas livré la soumission — basculer maintenant casserait l'entonnoir de réservation. **Six décisions de mise en œuvre amendent ADR-030** (§ Amendement du 2026-08-02). **(A) Colonne de sections dépliantes, pas un stepper** : six sections numérotées portant chacune son choix en résumé, à côté d'une scène collante ; l'écran 0 (filtre d'usage) est descendu en **section 05**, ce qui retire au stepper son seul avantage — il obligeait par ailleurs à naviguer pour comparer deux choix, ce que la scène rend immédiat. Le critère de recette §16 n°1 tient : `brancheFermee` retire la **barre de prix entière** sur la branche « terrain nu ». **(B) Coque de tunnel dans un groupe de routes dédié** `src/app/(configurateur)/` — une mise en page imbriquée s'ajoute à sa parente, elle ne peut pas en retirer la `<Nav>` ; ni méga-menu ni pied de page, une seule porte de sortie (le logo), `Analytics` + `CookieBanner` conservés pour le consentement (ADR-015). Entrée par les deux CTA « Réserver » du méga-menu Modules → `/configurer/v2?produit=one|max`, **paramètre lu côté serveur** (`useSearchParams` impose une frontière Suspense et fait échouer le prerender de production — le build Vercel est tombé dessus). **(C) Ambiances** : `visuel` et `teinte` déclarés dans la grille (`config.ts`), jamais dans un composant ; Littoral/Atelier/Basque réutilisent les rendus v1 (bleu pigeon / anthracite / vert), les trois images sont empilées et permutées en opacité — c'est le préchargement exigé au §14. Le sélecteur signale la sélection par la **teinte du bardage**, pas par l'accent. **(D) Mobile 390 px : c'est l'en-tête qui cède, pas le visuel** — il s'efface en descente et revient au premier geste vers le haut (seuil de 6 px, sans quoi l'inertie de Lenis le fait clignoter) ; la scène garde **232 px constants**. Une version qui la faisait rétrécir à 132 px a été **rejetée après essai** : `object-cover` coupait le pied du module sur un rendu 4:3. La réserve d'espace transite par une variable CSS `--cfg-nav`, seule couture entre la page et la mise en page. **(E) Compteur public** : « **2 séries · 12 exemplaires** ». **Volume maintenu à 12** — arbitrage Richard du 2026-08-02, qui **amende ADR-029 et le §5 de la spec** (ils fixaient 6 unités) ; `SERIE_TOTAL` et `serie.unites` sont alignés, le sélecteur de numéros en propose douze. **(F) Méthode** : le HMR Turbopack ne voit pas les modifications sur `/mnt/d` (WSL/NTFS) et `next build` local est trop lent — **le gate devient `tsc` + `eslint` + `check:vocabulaire`, puis la Preview Vercel**. Ce choix laisse une classe d'erreurs invisible avant push (le bailout `useSearchParams` n'est apparu qu'au prerender). **Trois arbitrages de Richard le 2026-08-02** : (1) **alerte Albert traitée verbalement**, écarts maintenus et assumés — 4 écarts à la spec (§8, §6-§7, §5 transport, §5 volume de série) plus le parti « colonne de sections » ; (2) **Série 01 reste à 12 unités**, ADR-029 amendée ; (3) **tous les CTA « Réserver » du site public pointent sur `/configurer/v2`** — méga-menu, en-tête, compteur, Hero, `AvantPremiere`, `ProductsShowcase`, `StickyCta`, bandeau de compte à rebours, pied de page et pages produit, via la fonction `reserverHref()` de `site.ts` pour que la bascule future tienne en une ligne. **⚠ Conséquence : l'entonnoir entier mène à un parcours dont le bouton final n'a pas de handler, et à une page `noindex` alors que `/configurer` reste au sitemap sans lien entrant — cet état ne doit pas atteindre `main` avant ADR-031.** **FAQ de l'accueil (« 012 — Questions ») reprise le même jour** : elle contredisait le v2 sur quatre points. **Réservation alignée à 2 000 €** partout (`DEPOSIT_EUR` 5 000 → 2 000, FAQ et bloc réassurance désormais **interpolés** sur la constante pour ne plus jamais diverger ; la variable Vercel `NEXT_PUBLIC_RESERVATION_DEPOSIT_EUR` est absente des trois scopes, vérifié — c'est le fallback du code qui sert en production, donc les CTA « Réserver — 2 000 € » suivent). **Mention « paiement sécurisé en ligne » retirée** — ADR-030 acte qu'aucun paiement n'est encaissé depuis le site, le lien de règlement part après l'échange de qualification. **Nouvelle question sur les options structurelles** (casquette, poêle, kit solaire non ajoutables après réservation, §5) : le configurateur le disait, la FAQ le taisait. **Échéancier 40/50/10 % laissé intact** — les pourcentages engagent contractuellement et relèvent des CGV, dont la version live n'est toujours pas confirmée par l'avocat (ADR-015) ; question non tranchée par Richard, recommandation appliquée par défaut. **« Et si je n'ai pas encore de terrain ? » conservée telle quelle** (décision Richard) : elle continue de dire que l'acquisition du terrain relève du client, ce qui **contredit sciemment ADR-029** (cadre restreint à l'annexe sur parcelle bâtie et à l'hébergement professionnel) sur la page la plus lue du site. **Reste ouvert** : ADR-031 (soumission), puis bascule sur `/configurer`, levée du `noindex` et retrait du v1.

**2026-08-01 (ADR-030 — configurateur v2 : parcours en 7 écrans + proposition d'interface)** — ADR-030 écrite et acceptée. Le configurateur passe d'un écran unique à un **tunnel en 7 écrans** (filtre d'usage, modèle, ambiance, terrasse, options, terrain, récapitulatif). **Le verrou ADR-005 est levé** : `Configurator.tsx` et `config-store.tsx` sont réécrits, `perM2` et `terrassePerM2` disparaissent au profit de grilles à paliers. La règle qui remplace « ne pas toucher » est **« ne jamais coder les grilles en dur »** — prix, paliers, options et volume de série restent éditables sans redéploiement (§12 : « elles bougeront », et §17.4 avertit que les montants d'options sont provisoires). ADR-005 et ADR-020 marquées **Remplacées**, guardrails de `CLAUDE.md` et `AGENTS.md` réécrits. **Écran 0 = filtre, pas confort** : la branche « terrain nu » n'affiche jamais de prix et termine le parcours (critère de recette §16 n°1). **Options** filtrées par le champ `modeles` — une option incompatible est *absente*, jamais grisée (§15) ; les 3 options structurelles sont groupées avec leur verrou « non modifiable après réservation ». **Transport par zone** = seul calcul automatique. **Proposition d'interface** livrée dans `docs/design/configurateur-v2.md` + maquette interactive (écrans 1→4 fonctionnels) : barre de prix ancrée, delta transitoire, paliers proportionnels sans prix au m², mention courte toujours visible + dépliant accessible au clavier. Mobile d'abord, cible **390 px**, cibles tactiles ≥ 48 px, budget 2,5 s en 4G. **Visuels actuels conservés** (décision Richard). **2 arbitrages ouverts**, câblés comme des drapeaux : nombre d'ambiances au lancement (§17.3) et bloc rentabilité dans le parcours particulier (§17.5). Les écrans 5 et 6 relèvent d'ADR-032 et ADR-031. **Amendée le même jour — 3 écarts assumés par rapport à la spec, arbitrages de Richard** : (1) la **pré-analyse PLU par adresse est conservée** à l'écran 1 (§8 l'interdisait — l'outil existe et rend deux services d'un seul appel, zonage + distance de transport) ; (2) **aucun paiement en ligne** (§6-§7) — le lien part après l'appel de qualification, mode **« demandé puis confirmé »** : le numéro n'est bloqué qu'à la confirmation du conseiller, l'arbitrage est humain ; (3) **transport au kilomètre** et non par zone (§5) — grutage 1 440 € + distance × poids × 0,24 €/t/km, soit 2,16 €/km sur le Max et 1,44 € sur le One, ce que la grille par zone ignorait. Écran 6 : CTA **« Réserver ce numéro »**, sélecteur des 6 numéros de la Série 01 (seul FOMO autorisé par le §6 — rareté réelle, jamais de compte à rebours), formulaire prénom/nom/téléphone/email tous obligatoires avec sélecteur d'indicatif drapeau, **deux cases jamais pré-cochées** (opt-in email facultatif, CGV obligatoire), CTA inactif tant que les CGV ne sont pas acceptées. Un numéro déjà demandé **reste libre et sélectionnable** ; l'information n'apparaît qu'après sélection — un 3ᵉ état sur la grille crée de l'hésitation au pire endroit du tunnel et brouille le compteur. **Alerte Albert passée à Oui** : ces 3 écarts touchent sa spécification et doivent lui être portés à connaissance. **Point à traiter dans ADR-031** : en mode demandé/confirmé, le compteur public peut afficher « 3 restants » alors que les trois sont déjà demandés — conforme à la lettre du §6 mais pas à la réalité commerciale ; garde-fous = mention « attribué à la signature du devis » et état `demande` exposé côté back-office.

**2026-07-31 (ADR-029 — repositionnement produit & marque, précondition du chantier configurateur v2)** — Albert a produit une spécification complète du configurateur (`docs/specs/SPEC_CONFIGURATEUR_HOWNER_v1.md`, v1 du 30/07, 17 sections, versée au dépôt en PR #63 avec l'accès Drive déclaré dans `project-access.json`). Le document **redéfinit le produit, le cadre de vente et le vocabulaire de marque**, bien au-delà du configurateur. **ADR-029 écrite et acceptée** : cadre de vente restreint à l'annexe sur parcelle bâtie et à l'hébergement professionnel — le logement indépendant sur terrain nu, promesse actuelle du site, est **fermé** (« prochainement », sans prix ni explication, critère de recette §16) ; vocabulaire inversé — « maison », « maison individuelle », « résidence principale », « clé en main » deviennent **interdits** (105 occurrences de « maison » dans le code), remplacés par module / unité / studio / hébergement / annexe ; prix **77 900 / 99 900 €**, réservation **2 000 €** + acompte 30 %, **Série 01 = 6 unités**. **ADR-004 est remplacée** par ADR-029 (ses règles « architecte intégrée » et « Puigbo » et sa blocklist historique sont reprises ; les deux blocklists sont cumulatives — « module » imposé, « modulaire » toujours interdit). Périmètre d'application : tout le site **sauf les pages légales** (§17.10 + risque ADR-015 ouvert). **Trois points d'arbitrage remontés à Howner** : (1) la spec se contredit sur « clé en main » — le §1 l'emploie, les §2/§12/§16 l'interdisent ; (2) « module » imposé vs « modulaire » interdit, lecture cumulative à confirmer ; (3) « une seule identité Howner, y compris mentions légales » se heurte à l'obligation légale de nommer l'éditeur réel, **Affinity House Factory SAS** — inapplicable à la lettre sans validation juridique. **Pas d'alerte Albert** : il est l'auteur de la décision. **Nouveau risque 🔴 consigné** : les prix publics sont faux de 18 000 € sur l'Arko One. Suite : ADR-030 → 034 (configurateur v2, créneaux & paiement, dossier terrain, back-office, espace client), plan de chantier en affinage.

**2026-07-31 (ADR-028 — correctif : le masquage des écrans admin ne tenait pas ; suspension vérifiée en production)** — Défaut constaté **en production** après le merge de la PR #58 : `/admin/mandataires`, `/admin/affectations`, `/admin/ged` et `/admin/terrains` renvoyaient **200** et le HTML servi contenait la **vraie page** (titre, boutons Inviter/Nouveau, liens de tri `?sort=zone_activite`, état de table) sous la page 404 — le composant serveur s'exécutait, **requêtes Supabase comprises**. Un visiteur voyait un 404 à l'écran, mais le contenu partait dans le payload RSC. **Cause** : `(admin)/admin/(protected)/layout.tsx` est un composant **client** (il porte la garde d'auth Supabase) ; il streame en premier, le statut 200 est figé, et le `notFound()` du layout serveur enfant arrive trop tard — il rend la page d'erreur mais n'annule ni le statut ni le payload déjà émis. **Correctif** (PR #59 → `dev`, PR #60 → `main`) : **`src/proxy.ts`** — Middleware renommé **Proxy** en Next 16 — intercepte les 4 chemins admin **avant tout rendu** et renvoie un 404 sec ; `guardMandataire()` ajouté en première instruction des **7 pages serveur** du périmètre en défense en profondeur (les 2 pages `"use client"` n'exécutent aucune requête serveur). Matcher strictement borné aux 4 chemins : `/admin` et `/admin/leads` restent intacts. **Les surfaces publiques, le portail mandataire et les 29 routes API étaient corrects dès #57** — 404 réel, aucun `<h1>`, aucun contenu de corps, aucune donnée servie ; seul l'objet `metadata` du segment reste évalué (Next l'évalue indépendamment de `notFound()`), cosmétique et sans fuite. **Vérifié en production après déploiement** : 4 écrans admin + sous-routes en **404 / `h1=0` / corps vide (0 octet)**, aucune fuite ; `/admin` et `/admin/leads` en 200 ; `/mandataire` `/terrains` `/rechercheterrain` `/terrain` `/cgu-mandataire` en 404 ; pages publiques et légales en 200 ; API en 404 ; sitemap 7 URLs. **Deux règles actées** et documentées (ADR-028 § « Une garde serveur sous un layout client ne suffit pas » + `docs/feature-flags.md`) : (1) une garde `notFound()` n'est fiable que si aucun layout client ne la précède dans l'arbre, sinon couper au proxy ; (2) **un code HTTP 404 ne prouve pas un masquage** — le contrôle `curl` vérifie désormais le corps servi (`<h1>`, taille) en plus du statut. **Alerte Albert faite verbalement** par Richard. **Reste** : test de réversibilité (`NEXT_PUBLIC_FEATURE_MANDATAIRE=true` sur une Preview) non exécuté.

**2026-07-30 (ADR-028 — suspension réversible du domaine « Mandataire & Terrain »)** — AHF suspend l'ensemble du dispositif mandataire/terrain : **finalité de marché et cible non mûres**. Le site promettait un service (« l'expert Mandataire Affinity vous re-contacte sous 48 h ») qu'AHF ne veut pas honorer aujourd'hui. **Suspension, pas suppression** : aucun fichier supprimé, aucune migration, aucune donnée ni aucun compte touché — seules les interfaces et les points d'entrée sont neutralisés, derrière un interrupteur unique `FEATURES.mandataire` (`src/lib/features.ts`, piloté par `NEXT_PUBLIC_FEATURE_MANDATAIRE` ; **variable absente = suspendu**, défaut sûr). Deux gardes : `guardMandataire()` (404 sur pages/layouts serveur) et `mandataireDisabled()` (404 sur API). **Coupé** : portail mandataire complet (une garde dans le layout de groupe couvre landing + auth + `(protected)`), onboarding, écrans admin Mandataires/Affectations/GED/Terrains (layouts de segment), sous-sections affectation + GED mandataire de la fiche lead, colonne Mandataire de la liste, widgets dashboard dérivés des dossiers, pages publiques `/terrains` `/rechercheterrain` `/terrain` `/cgu-mandataire`, liens Footer/NAV, CTA « Tester mon terrain » redirigés vers `/configurer`, sitemap/robots/llms.txt, mode « Je cherche un terrain » du configurateur, **29 routes API / 36 handlers**. **Conservé** : tunnel de réservation, pricing 3 couches intact, analyse PLU « J'ai un terrain », livraison GPS, `/contact`, Leads + fiche lead + **GED Client**, emails Brevo contact/récap. Amende **ADR-005** (UI configurateur touchée, verrou de pricing tenu), **ADR-018**, **ADR-025**, **ADR-027**. Effet de bord positif : `terrainMode` démarrant sur `"have"`, l'analyse PLU et le total livraison s'affichent sans clic préalable. `tsc` propre ; `eslint` : **322 erreurs avant / 322 après** (dette préexistante, zéro régression). **Alerte Albert** : retrait d'une offre commerciale du site public — changement de positionnement.

**2026-07-20 (SEO home — HTML front indexable : PR #54 → `dev` puis PR #55 → `main` mergées, ✅ LIVE en prod)** — Diagnostic : la home (et pages produit) étaient déjà rendues côté serveur, mais servies **invisibles** — framer-motion sérialisait `opacity:0` inline dans le HTML SSR (**23 blocs sur `/`, jusqu'à 64 sur `/arko-max`**) ; sans JS = page blanche. Hiérarchie Hn cassée (méga-menu `<h3>` avant le `<h1>`, footer `<h2>` dupliquant le `<h1>`). Correctif (6 fichiers front, **aucune migration**, aucun texte modifié) : `Reveal.tsx` réécrit sans framer-motion (`IntersectionObserver` + classes CSS, API publique inchangée → **Configurator ADR-005 non touché**) ; état masqué déplacé dans `globals.css` sous `.js-motion` (posée sur `<html>` uniquement si JS s'exécute → pas de JS = contenu visible/indexable) ; `<script>` inline brut dans `layout.tsx` pose `.js-motion` avant le premier paint (écarte `next/script beforeInteractive` = flash) ; `Hero.tsx` `<h1>` sorti du conteneur `opacity:0` (parallaxe image conservée en framer) ; `Nav.tsx`/`Footer.tsx` titres décoratifs re-balisés `<p>`. **Vérifié sur Preview Vercel** (Googlebot, sans JS) : `opacity:0` inline **23 → 0**, plan des titres **H1 → H2 → H3** imbriqué, contenu FAQ visible. `tsc` + `eslint` OK. **Formulaires sans JS** (question Richard) : normal et **non-bloquant SEO** (Googlebot exécute le JS ; un form n'est pas du contenu indexable) — `/configurer` rend son form au SSR, `/contact` affiche un fallback vide car le form lit l'URL (`useSearchParams` → `<Suspense>`), `/rechercheterrain` = page vitrine sans form (CTA → `/configurer`) ; refonte no-JS (Server Actions + anti-spam) non retenue. **Validation visuelle Preview OK**, PR #54 mergée sur `dev` puis PR #55 (`dev`→`main`) mergée le même jour — **le correctif est en production**, `dev` et `main` alignés, aucune migration associée. **Reste (non bloquant)** : SEO P2 polish ; amélioration optionnelle du squelette de form `/contact` sans JS.

**2026-07-13 (PR #24 `dev`→`main` mergée + migration Prod GED Client + ⚠️ ALERTE ALBERT légal)** — `main` mis à jour avec tout l'accumulé `dev` depuis le 6 juillet (v2.2 : GED dossiers, portail admin/mandataire, fiches terrain, dashboard KPIs, CGU, fix SEO domaine `howner.fr`, ADR-027 fiche lead, etc.). Migration `20260710_lead_client_documents.sql` appliquée sur Prod (`ahfhownerdb`, `msrjocrcewvqkcehruny`) — Preview et Prod alignés. **⚠️ ALERTE : la nouvelle version des CGV (`f3de62fe`, 9 juillet 2026) est désormais LIVE en production sur `/cgv`, et cette page est directement liée au checkbox d'acceptation dans le tunnel de réservation (`Reservation.tsx` L283/L470-471, `accepte_cgv: true`) — alors que la confirmation de l'avocat n'a jamais été obtenue (ADR-015 reste marqué « Bloqué »). Concrètement : un client réservant aujourd'hui accepte contractuellement un texte de CGV non validé juridiquement. Page en `noindex,follow` (invisible aux moteurs) mais parfaitement accessible et opposable pour tout visiteur/client. **Action requise avant toute nouvelle réservation ou communication commerciale** : obtenir la confirmation avocat en urgence, ou dépublier temporairement `/cgv` et bloquer le tunnel de réservation le temps de la validation. À remonter à Albert (alerte risque juridique, cf. CLAUDE.md).

**2026-07-10 (confirmation post-merge — `fix/scrape-annonce-error-logging` → `dev`, PR #51)** — Branche mergée dans `dev` : elle portait un scope bien plus large que son nom (ADR-027 fiche lead + refonte CGV + révision blocklist marque ADR-004 + refonte FAQ/hero/promesse/réassurance + fiabilisation import photos terrain + extraction IA Anthropic + sélecteur téléphone international + contact Brevo direct). **SPF/DKIM prod corrigé manuellement** — n'est plus un bloqueur (ADR-026 clos sur ce point). **Légal (ADR-015) reste bloqué uniquement sur la confirmation avocat de la nouvelle version CGV** (`f3de62fe`) — le reste du volet légal n'a pas de nouveau bloqueur. **Migration `20260710_lead_client_documents.sql` (GED Client) reste à appliquer** Preview/Prod. **Arko Max pricing grid toujours en attente** de données métier + grille tarifaire (aucun changement).

**2026-07-10 (ADR-027 — refonte fiche Lead admin : recherche terrain, affectation géo, GED double)** — Fiche `/admin/leads/[id]` réorganisée : suppression de « Configuration Arko » (redondante, `LeadConfigurateur.tsx` supprimé), « Affectation mandataire » et « Dossier mandataire » (renommé depuis « Dossier client ») déplacés en sous-sections de « Zone de recherche terrain ». **Matching géographique réel** (`AssignMandataire.tsx`) : Haversine `mandataires.lat/lon` vs `leads.plu_lat/lon`, rayon 200 km, tri exclusif puis distance (remplace l'ancien matching par substring sur `zone_activite`). Autocomplete Google Places sur « Adresse de recherche » (`LeadEditLocalisation.tsx`, même pattern que `LeadEditIdentite.tsx`). Deux nouveaux boutons email récap : mandataire affecté (`POST /api/admin/leads/[id]/affecter/recap`, réutilise `BREVO_TEMPLATE_AFFECTATION` + nouvelles variables `LEAD_DESCRIPTION`/`LEAD_PRODUIT`, lien direct `/mandataire/dossiers/{dossierId}`) et client (`POST /api/admin/leads/[id]/recap-client`, réutilise `BREVO_TEMPLATE_RECAP`, prépa devis futur). **Nouvelle GED Client** : table `lead_client_documents` (migration `20260710_lead_client_documents.sql`, à appliquer Preview/Prod), route `client-documents`, composant `LeadClientDocuments.tsx` — distincte de la GED mandataire (`lead_documents`). Détails : ADR-027. **Reste à faire** : ajouter les placeholders `LEAD_DESCRIPTION`/`LEAD_PRODUIT` au template Brevo `BREVO_TEMPLATE_AFFECTATION` ; génération du devis (hors scope de cet ADR).

**2026-06-30 (portail admin complet + mandataire redesign + PR #17 dev→main)** — **Portail admin Étapes 1→6** (PR #14 sur dev) : dashboard KPIs, liste+fiche leads, liste+fiche mandataires, affectations, carte Leaflet PLU, formulaire création lead/mandataire avec PLU + Pappers, matching zones, validation/suspension, invitation onboarding. **Portail mandataire redesign** (PR #16 sur dev) : landing publique `/mandataire` (pitch + 2 CTA), dashboard déplacé `/mandataire/dashboard`, signup épuré (sans landing step), mot de passe oublié (`forgot-password` + `reset-password` via Supabase Auth). **Template Brevo 15** créé (affectation lead → mandataire). **PR #17 `dev`→`main` en attente** : 2 migrations à appliquer post-merge (`20260629_admin_tables.sql`, `20260630_mandataires_invitation.sql`), var Vercel `BREVO_TEMPLATE_AFFECTATION=15` à ajouter, Supabase Redirect URL reset-password à configurer.

**2026-06-29 (Brevo contacts opt-in + portail mandataire finalisé + admin-portal Étapes 1+2)** — **Brevo opt-in (ADR-026 complété)** : `addBrevoContact()` direct (mandataires, list 7) + `addBrevoContactDOI()` DOI (prospects, list 8) dans `src/shared/lib/email.ts`. Checkbox opt-in dans `ContactForm.tsx` et `Reservation.tsx`. 3 routes API mises à jour (`/api/contact`, `/api/reservation`, `/api/recherche-terrain`). `/api/mandataire/notify-contrat` ajoute le mandataire direct en list 7 à la signature. PRs #12 (feat/brevo-contacts → dev) et #13 (dev → main) mergées. **Variables Vercel complétées** : `BREVO_TO_AHF`, `EMAIL_TO_AHF` (Preview), `BREVO_DOI_TEMPLATE_ID=13`, `BREVO_LIST_PROSPECTS=8`, `BREVO_LIST_MANDATAIRES=7`. **Migration `20260629_mandataires_documents_bucket.sql` appliquée sur prod** ✅. **`feat/admin-portal` Étapes 1+2 commitées** (branche locale, non pushée) : route group `(admin)`, signin (rôle `admin`), layout sidebar sombre (#1a1a18), dashboard KPIs (CA brut, revenus AHF, rémunérations, alertes 48h, donuts leads/dossiers, entonnoir, bar mandataires par perf), liste+fiche leads, liste+fiche mandataires, page affectations, grille article 4. Migration `20260629_admin_tables.sql` créée (**non encore appliquée**). `recharts` installé. Prochaine étape : Étapes 3-5 admin-portal (carte, matching, création lead/mandataire).

**2026-06-27 (câblage PackTerrain + bugfixes PLU + env Supabase 3 scopes)** — **Supabase 3 scopes Vercel** : vars Production (`ahfhownerdb`), Preview (`ahfhownerdb-preprod` / `ixozlavseaykxmjtkkrk`), Development configurées — `.env.local` resynchronisé. **6 migrations appliquées** preprod + prod via MCP Supabase OAuth (`apply_migration`). **PR `feat/terrain-address-lookup` mergée** ✅. **`PackTerrainContactForm` submit câblé** : `Configurator.tsx` écrit `pack_terrain_zones` en sessionStorage à chaque keystroke ; `Reservation.tsx` branche en mode pack vers `/api/recherche-terrain` (sans `incrementReserved` ni slot selector). **Fix livraison "À estimer"** : `ConfigRecap` réactif via événement DOM `plu_result_updated` (était one-shot au mount). **Fix `plu_adresse` NULL sur IDU path** : `reverseGeocode()` BAN lancé en `Promise.all` avec les appels GPU dans Voie B. **Fix champs PLU NULL en leads** : `PluConsentBlock` converti en `useEffect` + listener `plu_result_updated` + auto-check `onChange(true)` dès que la donnée arrive. **ADR-008 amendé** : Stripe retiré du MVP — paiement hors-ligne après qualification lead. **PR `fix/delivery-recap` mergée** ✅.

**2026-06-22 (feat/terrain-address-lookup — analyse PLU par adresse + calcul livraison GPS + schéma Supabase)** — Branche `feat/terrain-address-lookup` poussée (PR en attente de merge). `ParcelleAnalyse.tsx` : saisie par adresse (champ primaire) + bascule numéro de parcelle. Pipeline BAN → apicarto `geom=Point` → GPU `/feature-info/du?lon&lat&typeName` + `/feature-info/sup` (endpoint `/parcel/{id}/` abandonné — rejette tous formats). Fix `source_ign=PCI` (retournait aléatoirement 1000 parcelles Aix). `Reservation.tsx` : calcul livraison depuis `plu_result.lon/lat` sessionStorage (haversine × roadFactor 1.3 × poids × 0,24 €/t/km + grutage 1440 €). `TRANSPORT` dans `site.ts` (Arko One 6 t, Arko Max 9 t). `src/lib/supabase.ts` lazy client — résout `supabaseUrl is required` au build Preview. Migrations : `20260622_leads.sql` (table `leads` + RLS, colonnes PLU), `20260622_config_tarifs.sql` (`config_variables`, `options_produits`, `options_bardage`). `project-access.json` passé `read-write`. `docs/supabase-environments.md` créé (architecture Prod/Preview/Dev, scoping Vercel, workflow `feat/*`). ADR-022/025/026 mis à jour. Coordonnées atelier = placeholder Bayonne (43.4933, −1.4748) — à affiner. Question non tranchée : migrations automatiques GitHub Actions vs Supabase Branching.

**2026-06-22 (bugfix contact + contenu FAQ/footer)** — `ContactForm.tsx` : Turnstile invisible passé de l'auto-execute silencieux à `execute()` au clic (`execution: "execute"`) + pattern `pendingFormDataRef` (soumission relancée dans `onSuccess`). Handlers `onError`/`onExpire` : vident la file d'attente, reset loading, affichent l'erreur — plus de bouton silencieux. `/api/contact route.ts` : fallback `EMAIL_TO_AHF || BREVO_TO_AHF` (mismatch env Vercel — AHF ne recevait pas les notifications contact). FAQ (`src/lib/site.ts`, 5 questions) : réponses réécrites et détaillées — délai + 3 conditions suspensives de déclenchement fabrication, Pack Recherche Terrain 1 500 € + Mandataire Partenaire Howner-Affinity qualifié carte T + remboursement si terrain non trouvé, paiement 6 paliers décret 6 fév. 2020, garanties 1/2/10 ans + transférabilité décennale + DO obligatoire, après-vente GPA. Footer : « Fabriqué » → « **Fabriquées** » (accord féminin pluriel avec « deux maisons »), suppression « fondé par Puigbo ». DNS howner.fr non encore configuré → contact form non testable en conditions réelles (Turnstile rejette le headless Playwright). **ADR-026 reste** : migration SQL `contacts`, `PackTerrainContactForm` submit, SPF/DKIM prod.

**2026-06-20 (ADR-026 Emails Brevo + devops)** — Emails transactionnels livrés : `sendBrevoTemplate` (Brevo REST + `templateId` + `params` Jinja2, pas de SDK tiers). `/api/contact` câblé (Turnstile + Supabase `contacts` + Brevo template `10`). `/api/recherche-terrain` migré de Resend → Brevo template `9`. `ContactForm.tsx` : tous champs obligatoires (tel, produit select, email regex), bouton disabled sur `loading` uniquement. Bugfix : `<Devis>` wrappé dans `<Suspense>` (build cassé silencieusement). Devops : `WATCHPACK_POLLING=true` dans script `dev` (hotreload WSL2 NTFS). Packages retirés : `resend`, `@react-email/components`, `@react-email/render`. Clés Turnstile test configurées en dev (`1x00000000000000000000AA`). **Reste ADR-026** : migration SQL `contacts` (dashboard Supabase), `PackTerrainContactForm` sans submit, SPF/DKIM prod. ADR-014 fermé.

**2026-06-20 (ADR-025 Configurateur pack terrain + devops WSL2)** — Intégration pack terrain dans `/configurer` : champs villes/zones/département selon le pack Affinity sélectionné, champs contact (nom/tel/email) retirés du formulaire inline (récupérés à la réservation), case CGV déplacée juste avant le bouton Réserver. Section "Frais complémentaires Hors proposition (hors total)" avec nouvelles estimations. API `source` ajouté (`configurateur` / `rechercheterrain`). Migration `20260620_recherche_terrain_source.sql`. `Reservation.tsx` : label renommé + `ConfigRecap` détaillé par ligne (modèle/bardage/cuisine/barre/chambre/intérieur). Devops : `next.config.ts` `watchOptions.pollIntervalMs: 500` (corrige file-watching Turbopack sur `/mnt/d` WSL2). `.claude/settings.json` créé (`Edit(*)`, `Write(*)`, patterns Bash courants — réduit les prompts de validation). ADR-025 amendé.

**2026-06-17 (ADR-018 SEO P1 + pages légales)** — **SEO P1** livré (build vert, 17 routes) : JSON-LD via `src/lib/jsonld.ts` + `src/components/seo/JsonLd.tsx` — `Organization` sitewide (+ adresse AHF, founder Puigbo), `Product`+`Offer` sur `/arko-one` & `/arko-max` (prix par produit, `LimitedAvailability`), `FAQPage` sur la home. `/llms.txt` (route statique, suit `SITE_URL`). Logo Organization **omis** (charte non figée ADR-002), prix **exposé** (déjà public). **Pages légales remplies** : `mentions-legales` + `confidentialite` contenu réel mutualisé AHF (`LegalShell` rendu conditionnel `pending`/`updated`, style `.legal-prose`), passées `index:true` + ajoutées au sitemap ; **CGV reste placeholder noindex** (ADR-015). ⚠ **Alerte Albert RGPD** : la politique de confidentialité déclare GA4/cookies/Brevo + bandeau de consentement non déployés sur ce site (doc mutualisée multi-sites AHF) — à arbitrer avant mise en prod indexée. Blocklist marque OK. Reste **P2** (polish non bloquant).

**2026-06-17 (ADR-018 — SEO P0 livré)** — Socle SEO P0 implémenté et **vérifié au build** (`next build` vert, 16 routes statiques). Nouveaux fichiers : `src/app/sitemap.ts` (6 routes indexables, légal + `/viewer` exclus), `src/app/robots.ts` (`disallow /viewer` + host + sitemap), `src/app/opengraph-image.tsx` (généré `next/og`, 1200×630, tokens charte), `src/app/viewer/layout.tsx` (`noindex,nofollow`). `layout.tsx` : `metadataBase` recâblé + twitter card + canonical `/`. Canonical self-référent ajouté sur toutes les pages (légal inclus). **Domaine de prod confirmé = `affinityhome.fr`** (ex-`howner.fr`), centralisé dans `SITE_URL` (`src/lib/site.ts`, override `NEXT_PUBLIC_SITE_URL`). Incident env corrigé : binaire natif `lightningcss` manquant (WSL) → `npm install lightningcss-linux-x64-gnu`. **Reste** : P1 JSON-LD (Organization/Product+Offer/FAQPage) + `llms.txt`, P2 polish. ADR-018 → « Accepté — P0 livré ».

**2026-06-17 (ingestion `claude-knowledge` + audit projet)** — Base de connaissances officielle ingérée dans `~/.claude/rules/` (14 thèmes, 119 fichiers : `claude-code`, `components`, `design`, `discovery`, `landing`, `landing-sections`, `mcp`, `modules`, `notion`, `saas`, `spa`, `supabase`, `vercel`, `workflow`). `~/.claude/CLAUDE.md` remplacé par celui du repo (charge les règles via `@import`). Audit projet contre les règles : **`PROFIL.md` créé** à la racine (convention `rules/discovery/profil-md-convention.md`) + câblé en tête de `CLAUDE.md` projet. **ADR-001 amendé** : dérogation actée (la règle landing impose Astro, Howner reste Next.js car livré). **Dette SEO confirmée** comme priorité 1 (ADR-018). Aucun code applicatif modifié.

**2026-06-16 (refonte multi-pages bi-produit)** — Site passé en **multi-pages** (App Router) : `/`, `/arko-one`, `/arko-max`, `/configurer`, `/terrain`, `/contact`, `/cgv`, `/mentions-legales`, `/confidentialite`. **Bi-produit** Arko One (20 m², 12 ex, 59 900 €) + Arko Max (40 m², **5 ex**, 89 900 €) via registre `PRODUCTS` (`src/lib/site.ts`). **Nav type Tesla** (méga-menu Produits, compteur 12+5). Configurateur **multi-produit** (sélecteur One/Max, devis qui suit la base — ADR-020, amende 005). Wordmark ARKO retiré de l'accueil (baseline « Une maison compacte faite pour vous », « Fabriqué au Pays-Basque »). Pages légales = placeholders (bloqué ADR-015). ADR-020/021/022 créés. `tsc` propre, 10 routes en 200, console clean. **À fournir** : grille Arko One (perM2/options/dimensions), `reserved` par produit, asset vidéo Arko One (fallback provisoire = footage Max). **3 alertes Albert** : repositionnement bi-produit, déverrouillage configurateur, retrait wordmark ARKO. Charte Affinity (ADR-002) toujours à valider.
